{"dataset_version":"v1","pair_id":"js_0001","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/content-disposition","source_commit":"f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb","source_path":"index.js","source_start_line":144,"source_end_line":155,"source_url":"https://github.com/jshttp/content-disposition/blob/f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb/index.js#L144-L155","display_code":"function candidate (filename, options) {\n  var opts = options || {}\n\n  \n  var type = opts.type || 'attachment'\n\n  \n  var params = createparams(filename, opts.fallback)\n\n  \n  return format(new ContentDisposition(type, params))\n}","normalized_hash":"sha256-d235d762672f3b57fbdafb52b48d60e3de4a40e800683f7df1a602a7f8804d93"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:26:42.157Z","content":{"summary":"Builds and returns a formatted Content-Disposition header value for a supplied resource name and optional disposition settings.","signature_intent":"Accepts a resource name plus optional settings and produces a header-ready string.","behavior":["Treats missing settings as empty settings.","Uses the provided disposition kind when supplied.","Defaults the disposition kind to attachment when none is supplied.","Builds header parameters from the resource name and any alternate naming setting.","Returns the formatted header value."],"edge_cases":["Falsy settings are treated as absent.","Falsy disposition kind falls back to attachment.","Invalid resource names or settings may cause the underlying header construction to fail."],"forbidden_details":["Internal identifier names.","Repository or source location.","Unnecessary step-by-step implementation mechanics."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:27:12.735Z","display_code":"function candidate(resourceName, settings) {\n  const opts = settings || {};\n  const kind = opts.type || opts.disposition || opts.dispositionType || 'attachment';\n  const params = {};\n\n  if (resourceName !== undefined) {\n    if (typeof resourceName !== 'string') {\n      throw new TypeError('resource name must be a string');\n    }\n\n    const baseName = resourceName.split(/[\\\\/]/).pop();\n    const alternate = opts.fallback !== undefined ? opts.fallback : opts.alternate;\n    const asciiOnly = /^[\\x20-\\x7e]*$/.test(baseName);\n\n    if (typeof alternate === 'string') {\n      params.filename = alternate;\n      if (alternate !== baseName) {\n        params['filename*'] = \"UTF-8''\" + encodeURIComponent(baseName).replace(/[!'()]/g, c => '%' + c.charCodeAt(0).toString(16).toUpperCase());\n      }\n    } else if (alternate === false || asciiOnly) {\n      params.filename = baseName;\n    } else {\n      params.filename = baseName.replace(/[^\\x20-\\x7e]/g, '?');\n      params['filename*'] = \"UTF-8''\" + encodeURIComponent(baseName).replace(/[!'()]/g, c => '%' + c.charCodeAt(0).toString(16).toUpperCase());\n    }\n  }\n\n  const header = [String(kind).toLowerCase()];\n  for (const key of Object.keys(params).sort()) {\n    const value = params[key];\n    if (key.endsWith('*')) {\n      header.push(key + '=' + value);\n    } else {\n      header.push(key + '=\"' + String(value).replace(/[\\\\\"]/g, '\\\\$&') + '\"');\n    }\n  }\n\n  return header.join('; ');\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0002","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/content-disposition","source_commit":"f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb","source_path":"index.js","source_start_line":166,"source_end_line":215,"source_url":"https://github.com/jshttp/content-disposition/blob/f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb/index.js#L166-L215","display_code":"function candidate (filename, fallback) {\n  if (filename === undefined) {\n    return\n  }\n\n  var params = {}\n\n  if (typeof filename !== 'string') {\n    throw new TypeError('filename must be a string')\n  }\n\n  \n  if (fallback === undefined) {\n    fallback = true\n  }\n\n  if (typeof fallback !== 'string' && typeof fallback !== 'boolean') {\n    throw new TypeError('fallback must be a string or boolean')\n  }\n\n  if (typeof fallback === 'string' && NON_LATIN1_REGEXP.test(fallback)) {\n    throw new TypeError('fallback must be ISO-8859-1 string')\n  }\n\n  \n  var name = basename(filename)\n\n  \n  var isQuotedString = TEXT_REGEXP.test(name)\n\n  \n  var fallbackName = typeof fallback !== 'string'\n    ? fallback && getlatin1(name)\n    : basename(fallback)\n  var hasFallback = typeof fallbackName === 'string' && fallbackName !== name\n\n  \n  if (hasFallback || !isQuotedString || HEX_ESCAPE_REGEXP.test(name)) {\n    params['filename*'] = name\n  }\n\n  \n  if (isQuotedString || hasFallback) {\n    params.filename = hasFallback\n      ? fallbackName\n      : name\n  }\n\n  return params\n}","normalized_hash":"sha256-368a42350e6cc7749070609010b2ac4d59c278fa7cc8e645e8a18da1e7cc0e91"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:32:05.677Z","content":{"summary":"Builds a small attachment-name parameter object from a supplied path-like string and an optional plain-text fallback policy.","signature_intent":"Accepts a required string-like name input and an optional fallback control, returning undefined for an omitted name or an object of header-style name parameters.","behavior":["Returns undefined when the primary input is omitted.","Requires the primary input to be a string.","Defaults the fallback control to enabled when it is omitted.","Requires the fallback control to be either a boolean or a string.","Rejects a fallback string that cannot be represented as ISO-8859-1.","Uses only the final path segment of the supplied name.","Adds an extended name field when a plain quoted form is insufficient, when escaping is needed, or when a distinct fallback exists.","Adds a plain name field when the final name is safe for quoted use or when a distinct fallback exists.","When fallback is enabled as a boolean, derives a Latin-1-compatible fallback from the final name when possible.","When fallback is provided as a string, uses its final path segment as the plain fallback."],"edge_cases":["A disabled boolean fallback prevents generation of a derived fallback.","A fallback equal to the final primary name is treated as no distinct fallback.","An unsafe final name may produce only the extended name field when no distinct fallback is available.","A safe final name with no distinct fallback may produce only the plain name field.","A fallback string may include path separators; only its final segment is used."],"forbidden_details":["No variable names, repository names, source paths, or unnecessary implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:53:08.854Z","display_code":"function candidate(inputName, fallback) {\n  if (inputName === undefined) return undefined;\n  if (typeof inputName !== 'string') throw new TypeError('name must be a string');\n\n  const fallbackSetting = fallback === undefined ? true : fallback;\n  if (typeof fallbackSetting !== 'boolean' && typeof fallbackSetting !== 'string') {\n    throw new TypeError('fallback must be a boolean or string');\n  }\n\n  const splitLast = value => String(value).split(/[\\\\/]/).pop();\n  const hasNonLatin1 = value => /[^\\x00-\\xff]/.test(value);\n  const canBeQuoted = value => /^[\\x20-\\x7e\\x80-\\xff]*$/.test(value);\n  const needsPercentForm = value => /%[0-9a-fA-F]{2}/.test(value);\n\n  if (typeof fallbackSetting === 'string' && hasNonLatin1(fallbackSetting)) {\n    throw new TypeError('fallback must be ISO-8859-1 compatible');\n  }\n\n  const finalName = splitLast(inputName);\n  let plainAlternative;\n\n  if (typeof fallbackSetting === 'string') {\n    plainAlternative = splitLast(fallbackSetting);\n  } else if (fallbackSetting) {\n    plainAlternative = finalName.replace(/[^\\x00-\\xff]/g, '?');\n  }\n\n  const hasDistinctFallback = typeof plainAlternative === 'string' && plainAlternative !== finalName;\n  const safePlain = canBeQuoted(finalName);\n  const result = {};\n\n  if (hasDistinctFallback || !safePlain || needsPercentForm(finalName)) {\n    result['filename*'] = finalName;\n  }\n\n  if (safePlain || hasDistinctFallback) {\n    result.filename = hasDistinctFallback ? plainAlternative : finalName;\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0003","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/content-disposition","source_commit":"f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb","source_path":"index.js","source_start_line":227,"source_end_line":255,"source_url":"https://github.com/jshttp/content-disposition/blob/f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb/index.js#L227-L255","display_code":"function candidate (obj) {\n  var parameters = obj.parameters\n  var type = obj.type\n\n  if (!type || typeof type !== 'string' || !TOKEN_REGEXP.test(type)) {\n    throw new TypeError('invalid type')\n  }\n\n  \n  var string = String(type).toLowerCase()\n\n  \n  if (parameters && typeof parameters === 'object') {\n    var param\n    var params = Object.keys(parameters).sort()\n\n    for (var i = 0; i < params.length; i++) {\n      param = params[i]\n\n      var val = param.substr(-1) === '*'\n        ? ustring(parameters[param])\n        : qstring(parameters[param])\n\n      string += '; ' + param + '=' + val\n    }\n  }\n\n  return string\n}","normalized_hash":"sha256-97d763bccb442859b229234cc45b815a5537348dcbc075e854b97d2a0975f119"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:32:19.311Z","content":{"summary":"Serializes a structured token descriptor into a normalized semicolon-delimited string after validating the primary token.","signature_intent":"Accepts an object-like descriptor with a required token and optional attributes, returning a normalized string representation.","behavior":["Rejects a missing, non-string, or syntactically invalid primary token with a TypeError.","Lowercases the primary token in the returned value.","When optional attributes are present as an object, serializes them after the primary token.","Orders serialized attributes alphabetically by attribute key.","Formats each attribute as a key-value pair separated by an equals sign.","Uses quoted string formatting for normal attributes.","Uses unquoted string formatting for attributes whose key ends with an asterisk."],"edge_cases":["Falsy optional attributes are ignored.","Non-object optional attributes are ignored.","Attribute ordering is deterministic regardless of input order.","Invalid primary tokens fail before any attribute serialization occurs."],"forbidden_details":["Original identifier names.","Repository or source location.","Internal helper names beyond their observable formatting role.","Incidental implementation details not needed to describe behavior."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:53:25.401Z","display_code":"function candidate(input) {\n  if (!input || typeof input.token !== \"string\" || !/^[!#$%&'*+.^_`|~0-9A-Za-z-]+(?:\\/[!#$%&'*+.^_`|~0-9A-Za-z-]+)?$/.test(input.token)) {\n    throw new TypeError(\"invalid token\");\n  }\n\n  var output = input.token.toLowerCase();\n  var extras = input.attributes;\n\n  if (extras && typeof extras === \"object\" && !Array.isArray(extras)) {\n    Object.keys(extras).sort().forEach(function (key) {\n      var value = String(extras[key]);\n      if (key.charAt(key.length - 1) === \"*\") {\n        output += \"; \" + key + \"=\" + value;\n      } else {\n        output += \"; \" + key + \"=\\\"\" + value.replace(/[\\\\\"]/g, \"\\\\$&\") + \"\\\"\";\n      }\n    });\n  }\n\n  return output;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0004","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/content-disposition","source_commit":"f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb","source_path":"index.js","source_start_line":265,"source_end_line":291,"source_url":"https://github.com/jshttp/content-disposition/blob/f6d7cba7ea09dfea1492d5ffe438fe2f2e3cc3bb/index.js#L265-L291","display_code":"function candidate (str) {\n  var match = EXT_VALUE_REGEXP.exec(str)\n\n  if (!match) {\n    throw new TypeError('invalid extended field value')\n  }\n\n  var charset = match[1].toLowerCase()\n  var encoded = match[2]\n  var value\n\n  \n  var binary = encoded.replace(HEX_ESCAPE_REPLACE_REGEXP, pdecode)\n\n  switch (charset) {\n    case 'iso-8859-1':\n      value = getlatin1(binary)\n      break\n    case 'utf-8':\n      value = Buffer.from(binary, 'binary').toString('utf8')\n      break\n    default:\n      throw new TypeError('unsupported charset in extended field')\n  }\n\n  return value\n}","normalized_hash":"sha256-6748ba2300c727a55128feda6c3d305136dc6628cf8a73543e1efb284f3edec6"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:32:32.429Z","content":{"summary":"Parses an extended field value string and returns the decoded text when the declared character encoding is supported.","signature_intent":"Accepts a single string-form extended field value and returns a decoded string, or throws a TypeError.","behavior":["Validates that the input has the required extended field value format.","Treats the declared character encoding case-insensitively.","Decodes percent-escaped bytes before interpreting the text.","Supports ISO-8859-1 decoding.","Supports UTF-8 decoding.","Returns the decoded text on success."],"edge_cases":["Malformed input throws TypeError with an invalid-value message.","Unsupported character encoding throws TypeError with an unsupported-encoding message.","Uppercase or mixed-case supported encodings are accepted."],"forbidden_details":["Internal identifier names are omitted.","Repository and source path details are omitted.","Implementation-specific matching helpers are omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:53:47.093Z","display_code":"function candidate(input) {\n  if (typeof input !== \"string\") {\n    throw new TypeError(\"invalid-value\");\n  }\n\n  const first = input.indexOf(\"'\");\n  const second = first === -1 ? -1 : input.indexOf(\"'\", first + 1);\n\n  if (first <= 0 || second === -1) {\n    throw new TypeError(\"invalid-value\");\n  }\n\n  const encoding = input.slice(0, first).toLowerCase();\n  const payload = input.slice(second + 1);\n\n  if (encoding !== \"utf-8\" && encoding !== \"iso-8859-1\") {\n    throw new TypeError(\"unsupported-encoding\");\n  }\n\n  const bytes = [];\n\n  for (let i = 0; i < payload.length; i += 1) {\n    const ch = payload.charCodeAt(i);\n\n    if (payload[i] === \"%\") {\n      const hex = payload.slice(i + 1, i + 3);\n      if (!/^[0-9a-fA-F]{2}$/.test(hex)) {\n        throw new TypeError(\"invalid-value\");\n      }\n      bytes.push(parseInt(hex, 16));\n      i += 2;\n    } else {\n      if (ch > 0x7f) {\n        throw new TypeError(\"invalid-value\");\n      }\n      bytes.push(ch);\n    }\n  }\n\n  if (encoding === \"iso-8859-1\") {\n    return String.fromCharCode.apply(null, bytes);\n  }\n\n  if (typeof TextDecoder === \"function\") {\n    try {\n      return new TextDecoder(\"utf-8\", { fatal: true }).decode(new Uint8Array(bytes));\n    } catch (error) {\n      throw new TypeError(\"invalid-value\");\n    }\n  }\n\n  try {\n    return decodeURIComponent(bytes.map(function (byte) {\n      return \"%\" + byte.toString(16).padStart(2, \"0\");\n    }).join(\"\"));\n  } catch (error) {\n    throw new TypeError(\"invalid-value\");\n  }\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0006","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/cookie","source_commit":"aec1177c7da67e3b3273df96cf476824dbc9ae09","source_path":"index.js","source_start_line":49,"source_end_line":83,"source_url":"https://github.com/jshttp/cookie/blob/aec1177c7da67e3b3273df96cf476824dbc9ae09/index.js#L49-L83","display_code":"function candidate(str, options) {\n  if (typeof str !== 'string') {\n    throw new TypeError('argument str must be a string');\n  }\n\n  var obj = {}\n  var opt = options || {};\n  var pairs = str.split(pairSplitRegExp);\n  var dec = opt.decode || decode;\n\n  for (var i = 0; i < pairs.length; i++) {\n    var pair = pairs[i];\n    var eq_idx = pair.indexOf('=');\n\n    \n    if (eq_idx < 0) {\n      continue;\n    }\n\n    var key = pair.substr(0, eq_idx).trim()\n    var val = pair.substr(++eq_idx, pair.length).trim();\n\n    \n    if ('\"' == val[0]) {\n      val = val.slice(1, -1);\n    }\n\n    \n    if (undefined == obj[key]) {\n      obj[key] = tryDecode(val, dec);\n    }\n  }\n\n  return obj;\n}","normalized_hash":"sha256-dfc379d21ba2f1614b02ba70987c2680dc5b2dda0c7c7b1f7f8e274483b07c95"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:32:56.290Z","content":{"summary":"Parses a delimited assignment string into an object of decoded key-value entries.","signature_intent":"Accepts a string plus optional parsing settings and returns an object mapping parsed keys to decoded values.","behavior":["Requires the first argument to be a string.","Splits the input into candidate assignments using an internal pair separator.","Ignores segments that do not contain an equals sign.","Uses the text before the first equals sign as the key and the remaining text as the value.","Trims surrounding whitespace from parsed keys and values.","Removes wrapping double quotes from quoted values.","Decodes values with a provided decoder when available, otherwise with the default decoder.","Keeps the first value for a key and ignores later duplicates.","Returns an object containing the parsed entries."],"edge_cases":["Throws a TypeError when the first argument is not a string.","An absent settings argument uses default behavior.","Empty or separator-only input returns an empty object unless valid assignments are present.","Assignments with empty keys or empty values are still parsed.","Only the first equals sign separates the key from the value.","Duplicate keys preserve the earliest decoded value."],"forbidden_details":["Original parameter or local variable names.","Repository names or source file paths.","Internal helper names or regular expression identifiers.","Unnecessary implementation quirks not needed to describe behavior."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:54:36.740Z","display_code":"function candidate(input, settings) {\n  if (typeof input !== \"string\") {\n    throw new TypeError(\"argument must be a string\");\n  }\n\n  var result = {};\n  var options = settings || {};\n  var decodeValue = typeof options.decode === \"function\" ? options.decode : decodeURIComponent;\n  var pieces = input.split(/; */);\n\n  for (var index = 0; index < pieces.length; index += 1) {\n    var part = pieces[index];\n    var equalsAt = part.indexOf(\"=\");\n\n    if (equalsAt === -1) {\n      continue;\n    }\n\n    var name = part.slice(0, equalsAt).trim();\n\n    if (Object.prototype.hasOwnProperty.call(result, name)) {\n      continue;\n    }\n\n    var encoded = part.slice(equalsAt + 1).trim();\n\n    if (encoded.length >= 2 && encoded[0] === '\"' && encoded[encoded.length - 1] === '\"') {\n      encoded = encoded.slice(1, -1);\n    }\n\n    try {\n      result[name] = decodeValue(encoded);\n    } catch (error) {\n      result[name] = encoded;\n    }\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0007","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/cookie","source_commit":"aec1177c7da67e3b3273df96cf476824dbc9ae09","source_path":"index.js","source_start_line":101,"source_end_line":182,"source_url":"https://github.com/jshttp/cookie/blob/aec1177c7da67e3b3273df96cf476824dbc9ae09/index.js#L101-L182","display_code":"function candidate(name, val, options) {\n  var opt = options || {};\n  var enc = opt.encode || encode;\n\n  if (typeof enc !== 'function') {\n    throw new TypeError('option encode is invalid');\n  }\n\n  if (!fieldContentRegExp.test(name)) {\n    throw new TypeError('argument name is invalid');\n  }\n\n  var value = enc(val);\n\n  if (value && !fieldContentRegExp.test(value)) {\n    throw new TypeError('argument val is invalid');\n  }\n\n  var str = name + '=' + value;\n\n  if (null != opt.maxAge) {\n    var maxAge = opt.maxAge - 0;\n    if (isNaN(maxAge)) throw new Error('maxAge should be a Number');\n    str += '; Max-Age=' + Math.floor(maxAge);\n  }\n\n  if (opt.domain) {\n    if (!fieldContentRegExp.test(opt.domain)) {\n      throw new TypeError('option domain is invalid');\n    }\n\n    str += '; Domain=' + opt.domain;\n  }\n\n  if (opt.path) {\n    if (!fieldContentRegExp.test(opt.path)) {\n      throw new TypeError('option path is invalid');\n    }\n\n    str += '; Path=' + opt.path;\n  }\n\n  if (opt.expires) {\n    if (typeof opt.expires.toUTCString !== 'function') {\n      throw new TypeError('option expires is invalid');\n    }\n\n    str += '; Expires=' + opt.expires.toUTCString();\n  }\n\n  if (opt.httpOnly) {\n    str += '; HttpOnly';\n  }\n\n  if (opt.secure) {\n    str += '; Secure';\n  }\n\n  if (opt.sameSite) {\n    var sameSite = typeof opt.sameSite === 'string'\n      ? opt.sameSite.toLowerCase() : opt.sameSite;\n\n    switch (sameSite) {\n      case true:\n        str += '; SameSite=Strict';\n        break;\n      case 'lax':\n        str += '; SameSite=Lax';\n        break;\n      case 'strict':\n        str += '; SameSite=Strict';\n        break;\n      case 'none':\n        str += '; SameSite=None';\n        break;\n      default:\n        throw new TypeError('option sameSite is invalid');\n    }\n  }\n\n  return str;\n}","normalized_hash":"sha256-74d81fa4c2ad2ddafb27c542c9dd2496f2415d4f8dd3ef349cfa09eab3ce9bda"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:33:12.802Z","content":{"summary":"Serializes a cookie-style key/value pair with optional attributes into a single header-compatible string after validating inputs.","signature_intent":"Accepts an identifier, a value, and optional serialization settings; returns a formatted cookie string or throws on invalid input.","behavior":["Uses a default value encoder unless a custom encoder function is supplied.","Rejects a non-function custom encoder.","Rejects an invalid cookie identifier.","Encodes the supplied value before serialization.","Rejects a non-empty encoded value containing invalid header content.","Returns the base pair as identifier=value.","Adds Max-Age when provided, coercing it to a number and flooring it.","Adds Domain when provided and valid.","Adds Path when provided and valid.","Adds Expires when provided by calling its UTC string method.","Adds HttpOnly when enabled.","Adds Secure when enabled.","Adds SameSite as Strict, Lax, or None for accepted settings."],"edge_cases":["Missing settings are treated as empty settings.","Falsy optional attributes are omitted.","An empty encoded value is allowed.","A nonnumeric max age throws an error.","Invalid domain or path content throws a type error.","An expiration value without a UTC string method throws a type error.","SameSite true maps to Strict.","SameSite string matching is case-insensitive.","Unsupported SameSite values throw a type error."],"forbidden_details":["Internal identifier names.","Repository names.","Source paths.","Unnecessary implementation quirks."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:55:02.220Z","display_code":"function candidate(name, value, settings) {\n  const options = settings == null ? {} : settings;\n  const encoder = options.encode == null ? encodeURIComponent : options.encode;\n\n  if (typeof encoder !== \"function\") {\n    throw new TypeError(\"option encode is invalid\");\n  }\n\n  const tokenPattern = /^[!#$%&'*+\\-.^_`|~0-9A-Za-z]+$/;\n  if (typeof name !== \"string\" || !tokenPattern.test(name)) {\n    throw new TypeError(\"cookie name is invalid\");\n  }\n\n  const encoded = encoder(value);\n  const text = String(encoded);\n  const valuePattern = /^[\\x21\\x23-\\x2B\\x2D-\\x3A\\x3C-\\x5B\\x5D-\\x7E]*$/;\n  if (text !== \"\" && !valuePattern.test(text)) {\n    throw new TypeError(\"cookie value is invalid\");\n  }\n\n  let output = name + \"=\" + text;\n\n  if (options.maxAge) {\n    const seconds = Math.floor(Number(options.maxAge));\n    if (!Number.isFinite(seconds)) {\n      throw new TypeError(\"maxAge is invalid\");\n    }\n    output += \"; Max-Age=\" + seconds;\n  }\n\n  if (options.domain) {\n    const domain = String(options.domain);\n    const domainPattern = /^(?:[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?\\.)*[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?$/;\n    if (!domainPattern.test(domain)) {\n      throw new TypeError(\"domain is invalid\");\n    }\n    output += \"; Domain=\" + domain;\n  }\n\n  if (options.path) {\n    const path = String(options.path);\n    if (!/^[\\x20-\\x3A\\x3D-\\x7E]*$/.test(path)) {\n      throw new TypeError(\"path is invalid\");\n    }\n    output += \"; Path=\" + path;\n  }\n\n  if (options.expires) {\n    if (typeof options.expires.toUTCString !== \"function\") {\n      throw new TypeError(\"expires is invalid\");\n    }\n    output += \"; Expires=\" + options.expires.toUTCString();\n  }\n\n  if (options.httpOnly) {\n    output += \"; HttpOnly\";\n  }\n\n  if (options.secure) {\n    output += \"; Secure\";\n  }\n\n  if (options.sameSite) {\n    const sameSite = options.sameSite === true ? \"strict\" : String(options.sameSite).toLowerCase();\n    if (sameSite === \"strict\") {\n      output += \"; SameSite=Strict\";\n    } else if (sameSite === \"lax\") {\n      output += \"; SameSite=Lax\";\n    } else if (sameSite === \"none\") {\n      output += \"; SameSite=None\";\n    } else {\n      throw new TypeError(\"sameSite is invalid\");\n    }\n  }\n\n  return output;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0010","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":49,"source_end_line":68,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L49-L68","display_code":"function candidate (type) {\n  if (!type || typeof type !== 'string') {\n    return false\n  }\n\n  \n  var match = EXTRACT_TYPE_REGEXP.exec(type)\n  var mime = match && db[match[1].toLowerCase()]\n\n  if (mime && mime.charset) {\n    return mime.charset\n  }\n\n  \n  if (match && TEXT_TYPE_REGEXP.test(match[1])) {\n    return 'UTF-8'\n  }\n\n  return false\n}","normalized_hash":"sha256-e797c5ba66a2d01eef12954aa3069edd6da908ac973607353c1fb606c9b69eac"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:33:54.630Z","content":{"summary":"Determines the character encoding for a valid media type string, returning an explicit charset, a text default, or false.","signature_intent":"Accepts one value expected to be a media type string and returns a charset string or false.","behavior":["Returns false for missing or non-string input.","Looks up a known media type case-insensitively.","Returns the known charset when one is defined.","Returns UTF-8 for recognized text media types without a known charset.","Returns false when no charset can be determined."],"edge_cases":["Empty strings return false.","Non-string values return false.","Unknown media types return false.","Known media types without charset only default to UTF-8 when they are text types.","Case differences in the media type do not prevent known-type lookup."],"forbidden_details":["Variable names","Repository names","Source paths","Regular expression identifiers","Internal lookup object names"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:56:04.990Z","display_code":"function candidate(value) {\n  if (typeof value !== \"string\") return false;\n\n  const trimmed = value.trim();\n  if (trimmed === \"\") return false;\n\n  const typePart = trimmed.split(\";\", 1)[0].trim().toLowerCase();\n  if (!/^[!#$%&'*+.^_`|~0-9a-z-]+\\/[!#$%&'*+.^_`|~0-9a-z-]+$/.test(typePart)) return false;\n\n  const charsets = {\n    \"application/ecmascript\": \"UTF-8\",\n    \"application/javascript\": \"UTF-8\",\n    \"application/json\": \"UTF-8\",\n    \"application/ld+json\": \"UTF-8\",\n    \"application/manifest+json\": \"UTF-8\",\n    \"application/rss+xml\": \"UTF-8\",\n    \"application/atom+xml\": \"UTF-8\",\n    \"application/wasm\": \"UTF-8\",\n    \"application/x-javascript\": \"UTF-8\",\n    \"application/xhtml+xml\": \"UTF-8\",\n    \"application/xml\": \"UTF-8\",\n    \"image/svg+xml\": \"UTF-8\"\n  };\n\n  if (Object.prototype.hasOwnProperty.call(charsets, typePart)) {\n    return charsets[typePart];\n  }\n\n  const knownText = new Set([\n    \"text/cache-manifest\",\n    \"text/calendar\",\n    \"text/css\",\n    \"text/csv\",\n    \"text/html\",\n    \"text/javascript\",\n    \"text/markdown\",\n    \"text/plain\",\n    \"text/tab-separated-values\",\n    \"text/xml\"\n  ]);\n\n  if (knownText.has(typePart)) return \"UTF-8\";\n\n  return false;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0011","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":77,"source_end_line":98,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L77-L98","display_code":"function candidate (str) {\n  \n  if (!str || typeof str !== 'string') {\n    return false\n  }\n\n  var mime = str.indexOf('/') === -1\n    ? exports.lookup(str)\n    : str\n\n  if (!mime) {\n    return false\n  }\n\n  \n  if (mime.indexOf('charset') === -1) {\n    var charset = exports.charset(mime)\n    if (charset) mime += '; charset=' + charset.toLowerCase()\n  }\n\n  return mime\n}","normalized_hash":"sha256-9351381002afa6db0951c3acf36fe820ca7c0db7b1134ee8be880962394f1653"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:34:08.990Z","content":{"summary":"Validates a string as either a media type or resolvable extension, returning a media type string with a default charset when applicable.","signature_intent":"Accepts one value and returns either false or a media type string.","behavior":["Returns false for missing or non-string input.","Accepts media type strings directly.","Resolves extension-like strings to media types.","Returns false when no media type can be determined.","Adds a charset parameter when one is not already present and a default charset is known.","Returns the final media type string."],"edge_cases":["Empty strings return false.","Unknown extensions return false.","Existing charset information is preserved without adding another charset.","Added charset values are lowercase."],"forbidden_details":["Variable names","Repository names","Source paths","Unnecessary implementation quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:56:24.716Z","display_code":"function candidate(value) {\n  if (typeof value !== \"string\" || value.length === 0) return false;\n\n  const known = {\n    html: \"text/html\",\n    htm: \"text/html\",\n    txt: \"text/plain\",\n    text: \"text/plain\",\n    css: \"text/css\",\n    csv: \"text/csv\",\n    js: \"application/javascript\",\n    mjs: \"application/javascript\",\n    json: \"application/json\",\n    map: \"application/json\",\n    xml: \"application/xml\",\n    svg: \"image/svg+xml\",\n    png: \"image/png\",\n    jpg: \"image/jpeg\",\n    jpeg: \"image/jpeg\",\n    gif: \"image/gif\",\n    webp: \"image/webp\",\n    pdf: \"application/pdf\",\n    wasm: \"application/wasm\"\n  };\n\n  const isType = /^[^\\s/;]+\\/[^\\s;]+(?:\\s*;\\s*[^=;\\s]+=[^;]+)*$/.test(value);\n  let result = isType ? value : known[value.replace(/^\\.+/, \"\").toLowerCase()];\n\n  if (!result) return false;\n\n  if (!/(?:^|;)\\s*charset\\s*=/.test(result)) {\n    const base = result.split(\";\", 1)[0].trim().toLowerCase();\n    let fallback = null;\n\n    if (base.startsWith(\"text/\")) fallback = \"utf-8\";\n    else if (base === \"application/json\" || base.endsWith(\"+json\")) fallback = \"utf-8\";\n    else if (base === \"application/javascript\" || base === \"application/xml\" || base.endsWith(\"+xml\")) fallback = \"utf-8\";\n\n    if (fallback) result += \"; charset=\" + fallback;\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0012","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":107,"source_end_line":123,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L107-L123","display_code":"function candidate (type) {\n  if (!type || typeof type !== 'string') {\n    return false\n  }\n\n  \n  var match = EXTRACT_TYPE_REGEXP.exec(type)\n\n  \n  var exts = match && exports.extensions[match[1].toLowerCase()]\n\n  if (!exts || !exts.length) {\n    return false\n  }\n\n  return exts[0]\n}","normalized_hash":"sha256-a558397383c31076b64e05efcde86bcc017e41f409c5e963b7d792ae95320ded"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:34:21.175Z","content":{"summary":"Determines whether a supplied value names a known content type and returns its primary associated file extension, otherwise returning false.","signature_intent":"Accepts one value and returns either a string file extension or boolean false.","behavior":["Rejects missing, empty, or non-string input with false.","Extracts the relevant content-type identifier from a valid string input.","Matches identifiers case-insensitively against known extension entries.","Returns the first extension associated with a recognized identifier.","Returns false when no known extensions are available for the identifier."],"edge_cases":["Falsy strings are rejected with false.","Non-string values are rejected with false.","Unparseable strings return false.","Recognized identifiers with no extensions return false."],"forbidden_details":[]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:56:53.885Z","display_code":"function candidate(value) {\n  if (!value || typeof value !== \"string\") return false;\n\n  var found = /^\\s*([^;\\s]+)(?:[;\\s]|$)/.exec(value);\n  if (!found) return false;\n\n  var type = found[1].toLowerCase();\n  var extensions = {\n    \"application/atom+xml\": [\"atom\"],\n    \"application/ecmascript\": [\"ecma\"],\n    \"application/gzip\": [\"gz\"],\n    \"application/javascript\": [\"js\"],\n    \"application/json\": [\"json\"],\n    \"application/ld+json\": [\"jsonld\"],\n    \"application/msword\": [\"doc\"],\n    \"application/octet-stream\": [\"bin\"],\n    \"application/ogg\": [\"ogx\"],\n    \"application/pdf\": [\"pdf\"],\n    \"application/postscript\": [\"ai\"],\n    \"application/rtf\": [\"rtf\"],\n    \"application/vnd.ms-excel\": [\"xls\"],\n    \"application/vnd.ms-powerpoint\": [\"ppt\"],\n    \"application/vnd.oasis.opendocument.presentation\": [\"odp\"],\n    \"application/vnd.oasis.opendocument.spreadsheet\": [\"ods\"],\n    \"application/vnd.oasis.opendocument.text\": [\"odt\"],\n    \"application/vnd.openxmlformats-officedocument.presentationml.presentation\": [\"pptx\"],\n    \"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\": [\"xlsx\"],\n    \"application/vnd.openxmlformats-officedocument.wordprocessingml.document\": [\"docx\"],\n    \"application/wasm\": [\"wasm\"],\n    \"application/xhtml+xml\": [\"xhtml\"],\n    \"application/xml\": [\"xml\"],\n    \"application/zip\": [\"zip\"],\n    \"audio/aac\": [\"aac\"],\n    \"audio/midi\": [\"mid\"],\n    \"audio/mp3\": [\"mp3\"],\n    \"audio/mpeg\": [\"mpga\"],\n    \"audio/ogg\": [\"oga\"],\n    \"audio/wav\": [\"wav\"],\n    \"audio/webm\": [\"weba\"],\n    \"font/otf\": [\"otf\"],\n    \"font/ttf\": [\"ttf\"],\n    \"font/woff\": [\"woff\"],\n    \"font/woff2\": [\"woff2\"],\n    \"image/apng\": [\"apng\"],\n    \"image/avif\": [\"avif\"],\n    \"image/bmp\": [\"bmp\"],\n    \"image/gif\": [\"gif\"],\n    \"image/jpeg\": [\"jpeg\"],\n    \"image/png\": [\"png\"],\n    \"image/svg+xml\": [\"svg\"],\n    \"image/tiff\": [\"tiff\"],\n    \"image/webp\": [\"webp\"],\n    \"multipart/form-data\": [],\n    \"text/calendar\": [\"ics\"],\n    \"text/css\": [\"css\"],\n    \"text/csv\": [\"csv\"],\n    \"text/html\": [\"html\"],\n    \"text/javascript\": [\"js\"],\n    \"text/markdown\": [\"markdown\"],\n    \"text/plain\": [\"txt\"],\n    \"text/xml\": [\"xml\"],\n    \"video/mp4\": [\"mp4\"],\n    \"video/mpeg\": [\"mpeg\"],\n    \"video/ogg\": [\"ogv\"],\n    \"video/quicktime\": [\"mov\"],\n    \"video/webm\": [\"webm\"],\n    \"video/x-msvideo\": [\"avi\"]\n  }[type];\n\n  return extensions && extensions.length ? extensions[0] : false;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0013","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":132,"source_end_line":147,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L132-L147","display_code":"function candidate (path) {\n  if (!path || typeof path !== 'string') {\n    return false\n  }\n\n  \n  var extension = extname('x.' + path)\n    .toLowerCase()\n    .substr(1)\n\n  if (!extension) {\n    return false\n  }\n\n  return exports.types[extension] || false\n}","normalized_hash":"sha256-d092c6450f9ef450046e8cce98f2779657e4059d220382d09c28797d5e78d72e"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:34:29.407Z","content":{"summary":"Determines whether a string has a recognized file extension and returns the associated type or false.","signature_intent":"Accepts a value expected to be a string containing a file extension or filename, and returns a mapped type value or false.","behavior":["Returns false for non-string or empty input.","Extracts the final file extension from the input.","Matches extensions case-insensitively.","Returns the registered type value when the extension is recognized.","Returns false when the extension is missing or unrecognized."],"edge_cases":["Null, undefined, false, numbers, objects, and arrays return false.","Strings without an extension return false.","Uppercase or mixed-case extensions are normalized before matching.","Recognized extensions return their mapped value exactly."],"forbidden_details":["Internal variable names.","Repository names.","Source file paths.","Implementation-specific parsing quirks."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:57:46.832Z","display_code":"function candidate(value) {\n  if (typeof value !== \"string\" || value.length === 0) {\n    return false;\n  }\n\n  var types = {\n    html: \"text/html\",\n    htm: \"text/html\",\n    shtml: \"text/html\",\n    js: \"application/javascript\",\n    mjs: \"application/javascript\",\n    json: \"application/json\",\n    map: \"application/json\",\n    rtf: \"application/rtf\",\n    txt: \"text/plain\",\n    text: \"text/plain\",\n    conf: \"text/plain\",\n    def: \"text/plain\",\n    list: \"text/plain\",\n    log: \"text/plain\",\n    in: \"text/plain\",\n    ini: \"text/plain\",\n    xml: \"application/xml\",\n    xsl: \"application/xml\",\n    xsd: \"application/xml\",\n    css: \"text/css\",\n    csv: \"text/csv\",\n    md: \"text/markdown\",\n    markdown: \"text/markdown\",\n    pdf: \"application/pdf\",\n    zip: \"application/zip\",\n    gz: \"application/gzip\",\n    gif: \"image/gif\",\n    jpg: \"image/jpeg\",\n    jpeg: \"image/jpeg\",\n    png: \"image/png\",\n    svg: \"image/svg+xml\",\n    webp: \"image/webp\",\n    ico: \"image/vnd.microsoft.icon\",\n    mp3: \"audio/mpeg\",\n    mp4: \"video/mp4\",\n    mpeg: \"video/mpeg\",\n    webm: \"video/webm\",\n    wasm: \"application/wasm\"\n  };\n\n  var normalized = value.toLowerCase();\n  var slash = Math.max(normalized.lastIndexOf(\"/\"), normalized.lastIndexOf(\"\\\\\"));\n  var name = normalized.slice(slash + 1);\n  var dot = name.lastIndexOf(\".\");\n  var suffix = dot === -1 ? name : name.slice(dot + 1);\n\n  if (!suffix || (dot === 0 && slash !== -1 && name.indexOf(\".\", 1) === -1)) {\n    return false;\n  }\n\n  return types[suffix] || false;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0014","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":154,"source_end_line":188,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L154-L188","display_code":"function candidate (extensions, types) {\n  \n  var preference = ['nginx', 'apache', undefined, 'iana']\n\n  Object.keys(db).forEach(function forEachMimeType (type) {\n    var mime = db[type]\n    var exts = mime.extensions\n\n    if (!exts || !exts.length) {\n      return\n    }\n\n    \n    extensions[type] = exts\n\n    \n    for (var i = 0; i < exts.length; i++) {\n      var extension = exts[i]\n\n      if (types[extension]) {\n        var from = preference.indexOf(db[types[extension]].source)\n        var to = preference.indexOf(mime.source)\n\n        if (types[extension] !== 'application/octet-stream' &&\n          (from > to || (from === to && types[extension].substr(0, 12) === 'application/'))) {\n          \n          continue\n        }\n      }\n\n      \n      types[extension] = type\n    }\n  })\n}","normalized_hash":"sha256-608457d18b317152d67ee67fe53f918f97540573c4be0de61f083ee1a489e0d9"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:34:45.179Z","content":{"summary":"Builds lookup tables that associate media types with extensions and extensions with their selected media type.","signature_intent":"Accepts two mutable lookup objects and fills them using a shared media metadata registry.","behavior":["Adds each media type's extension list to the media-type lookup when extensions are available.","Adds an extension-to-media-type mapping for each listed extension.","When multiple media types share an extension, keeps an existing mapping if it has a higher-ranked metadata source.","For equal metadata ranking, keeps a non-generic existing application media type over a later application media type.","Allows the generic binary media type to be replaced by a later matching candidate."],"edge_cases":["Entries without extensions are skipped.","Empty extension lists are skipped.","Existing mappings may remain unchanged when a later candidate is lower ranked.","Generic binary mappings do not block replacement."],"forbidden_details":["Variable names","Repository names","Source paths","Incidental loop mechanics"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:58:14.331Z","display_code":"function candidate(mediaTypeLookup, extensionLookup) {\n  var registry = typeof db !== 'undefined' ? db : typeof mimeDb !== 'undefined' ? mimeDb : typeof mimeDatabase !== 'undefined' ? mimeDatabase : null;\n  var sourceOrder = { nginx: 0, apache: 1, undefined: 2, iana: 3 };\n\n  Object.keys(registry || {}).forEach(function (mediaType) {\n    var metadata = registry[mediaType];\n    var suffixes = metadata && metadata.extensions;\n\n    if (!suffixes || suffixes.length === 0) {\n      return;\n    }\n\n    mediaTypeLookup[mediaType] = suffixes;\n\n    suffixes.forEach(function (suffix) {\n      var currentType = extensionLookup[suffix];\n\n      if (currentType) {\n        var currentMetadata = registry[currentType] || {};\n        var currentRank = sourceOrder[String(currentMetadata.source)];\n        var nextRank = sourceOrder[String(metadata.source)];\n\n        if (currentRank === undefined) {\n          currentRank = sourceOrder.undefined;\n        }\n        if (nextRank === undefined) {\n          nextRank = sourceOrder.undefined;\n        }\n\n        if (currentType !== 'application/octet-stream' &&\n            (currentRank > nextRank ||\n              (currentRank === nextRank && currentType.indexOf('application/') === 0))) {\n          return;\n        }\n      }\n\n      extensionLookup[suffix] = mediaType;\n    });\n  });\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0015","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"jshttp/mime-types","source_commit":"47b62ac45e9b176a2af35532d0eea4968bb9eb6d","source_path":"index.js","source_start_line":158,"source_end_line":187,"source_url":"https://github.com/jshttp/mime-types/blob/47b62ac45e9b176a2af35532d0eea4968bb9eb6d/index.js#L158-L187","display_code":"function candidate (type) {\n    var mime = db[type]\n    var exts = mime.extensions\n\n    if (!exts || !exts.length) {\n      return\n    }\n\n    \n    extensions[type] = exts\n\n    \n    for (var i = 0; i < exts.length; i++) {\n      var extension = exts[i]\n\n      if (types[extension]) {\n        var from = preference.indexOf(db[types[extension]].source)\n        var to = preference.indexOf(mime.source)\n\n        if (types[extension] !== 'application/octet-stream' &&\n          (from > to || (from === to && types[extension].substr(0, 12) === 'application/'))) {\n          \n          continue\n        }\n      }\n\n      \n      types[extension] = type\n    }\n  }","normalized_hash":"sha256-aa367b789103c0d405a88cfd92daa3c6b9d45c53b86f4c7988c9c691238e4f2a"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:34:55.255Z","content":{"summary":"Registers file suffix mappings for a known content type when suffix metadata exists.","signature_intent":"Accepts a content type identifier and updates shared lookup mappings as a side effect.","behavior":["Stops without changes when the content type has no associated suffixes.","Stores the content type's suffix list for reverse lookup.","Assigns each suffix to the content type when no better existing mapping is present.","Preserves an existing suffix mapping when it has higher configured priority.","On equal priority, preserves an existing application-class mapping over another competing mapping."],"edge_cases":["Missing or empty suffix metadata produces no result.","A generic binary mapping can be replaced by a more specific content type.","Existing mappings may remain unchanged when priority rules favor them."],"forbidden_details":["Variable names, repository names, source paths, and incidental loop mechanics are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:58:29.408Z","display_code":"function candidate(type) {\n  const record = db[type];\n  if (!record || !record.extensions || record.extensions.length === 0) return;\n\n  extensions[type] = record.extensions;\n\n  const rank = [\"nginx\", \"apache\", undefined, \"iana\"];\n\n  for (const suffix of record.extensions) {\n    const current = types[suffix];\n\n    if (current) {\n      const currentRank = rank.indexOf(db[current] && db[current].source);\n      const nextRank = rank.indexOf(record.source);\n\n      if (current !== \"application/octet-stream\" && (currentRank > nextRank || (currentRank === nextRank && current.slice(0, 12) === \"application/\"))) {\n        continue;\n      }\n    }\n\n    types[suffix] = type;\n  }\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0016","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":34,"source_end_line":97,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L34-L97","display_code":"function candidate(path, allowAboveRoot) {\n  var res = '';\n  var lastSegmentLength = 0;\n  var lastSlash = -1;\n  var dots = 0;\n  var code;\n  for (var i = 0; i <= path.length; ++i) {\n    if (i < path.length)\n      code = path.charCodeAt(i);\n    else if (code === 47 )\n      break;\n    else\n      code = 47 ;\n    if (code === 47 ) {\n      if (lastSlash === i - 1 || dots === 1) {\n        \n      } else if (lastSlash !== i - 1 && dots === 2) {\n        if (res.length < 2 || lastSegmentLength !== 2 || res.charCodeAt(res.length - 1) !== 46  || res.charCodeAt(res.length - 2) !== 46 ) {\n          if (res.length > 2) {\n            var lastSlashIndex = res.lastIndexOf('/');\n            if (lastSlashIndex !== res.length - 1) {\n              if (lastSlashIndex === -1) {\n                res = '';\n                lastSegmentLength = 0;\n              } else {\n                res = res.slice(0, lastSlashIndex);\n                lastSegmentLength = res.length - 1 - res.lastIndexOf('/');\n              }\n              lastSlash = i;\n              dots = 0;\n              continue;\n            }\n          } else if (res.length === 2 || res.length === 1) {\n            res = '';\n            lastSegmentLength = 0;\n            lastSlash = i;\n            dots = 0;\n            continue;\n          }\n        }\n        if (allowAboveRoot) {\n          if (res.length > 0)\n            res += '/..';\n          else\n            res = '..';\n          lastSegmentLength = 2;\n        }\n      } else {\n        if (res.length > 0)\n          res += '/' + path.slice(lastSlash + 1, i);\n        else\n          res = path.slice(lastSlash + 1, i);\n        lastSegmentLength = i - lastSlash - 1;\n      }\n      lastSlash = i;\n      dots = 0;\n    } else if (code === 46  && dots !== -1) {\n      ++dots;\n    } else {\n      dots = -1;\n    }\n  }\n  return res;\n}","normalized_hash":"sha256-ba8f3eb7340d7dba993091701c7ee69da389c3e48b5b67711dae7fbd5d1892a7"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:35:13.499Z","content":{"summary":"Normalizes a slash-delimited relative path by removing redundant separators and current-directory segments while resolving parent-directory segments according to a boolean policy.","signature_intent":"Accepts a POSIX-style relative path string and a boolean that controls whether unresolved leading parent traversals are preserved; returns a normalized relative path string.","behavior":["Uses forward slash as the only segment separator.","Collapses repeated separators.","Removes current-directory segments.","Resolves parent-directory segments by removing the preceding normal segment when possible.","Preserves unresolved parent-directory segments only when the boolean option permits it.","Returns a relative normalized string without preserving a trailing separator."],"edge_cases":["Empty input returns an empty string.","Inputs containing only separators or current-directory segments return an empty string.","Leading parent-directory segments are dropped when preservation is disabled.","Leading or excess parent-directory segments are retained when preservation is enabled.","Dot-prefixed names other than a single or double dot are treated as normal segments.","Parent traversal does not cancel a previously preserved unresolved parent segment."],"forbidden_details":["Implementation identifier names.","Repository names.","Source file paths.","Low-level loop mechanics or character-code details."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:58:40.993Z","display_code":"function candidate(path, keepParents) {\n  if (path === \"\") return \"\";\n\n  const output = [];\n  const parts = path.split(\"/\");\n\n  for (const part of parts) {\n    if (part === \"\" || part === \".\") continue;\n\n    if (part === \"..\") {\n      const last = output[output.length - 1];\n      if (last && last !== \"..\") {\n        output.pop();\n      } else if (keepParents) {\n        output.push(\"..\");\n      }\n    } else {\n      output.push(part);\n    }\n  }\n\n  return output.join(\"/\");\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0017","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":99,"source_end_line":109,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L99-L109","display_code":"function candidate(sep, pathObject) {\n  var dir = pathObject.dir || pathObject.root;\n  var base = pathObject.base || (pathObject.name || '') + (pathObject.ext || '');\n  if (!dir) {\n    return base;\n  }\n  if (dir === pathObject.root) {\n    return dir + base;\n  }\n  return dir + sep + base;\n}","normalized_hash":"sha256-ea0e2814bf2efa277e79584c1fca62fb3ec4d9dc93dc7b1d4afdeb90f47084d5"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:35:32.950Z","content":{"summary":"Builds a path string from parsed path components, preferring explicit directory and filename values and otherwise composing missing filename text from stem plus extension.","signature_intent":"Accepts a path separator and an object containing parsed path components; returns the formatted path string.","behavior":["Selects the directory component when present, otherwise the root component.","Selects the complete filename when present, otherwise joins the stem and extension components, treating missing parts as empty text.","Returns only the filename text when no directory or root component is available.","When the selected directory text exactly matches the root text, concatenates it directly with the filename text.","Otherwise inserts the supplied separator between the selected directory text and filename text."],"edge_cases":["Falsy directory and root values produce a filename-only result.","A falsy complete filename is replaced by stem-plus-extension composition.","Missing stem or extension components are treated as empty strings for filename composition.","If the selected directory equals the root exactly, no additional separator is inserted."],"forbidden_details":["Original identifiers are intentionally omitted.","Repository and source-location details are intentionally omitted.","Incidental implementation quirks are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:58:50.641Z","display_code":"function candidate(separator, parts) {\n  const folderText = parts.dir || parts.root;\n  const fileText = parts.base || `${parts.name || \"\"}${parts.ext || \"\"}`;\n\n  if (!folderText) {\n    return fileText;\n  }\n\n  return folderText === parts.root\n    ? folderText + fileText\n    : folderText + separator + fileText;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0018","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":113,"source_end_line":155,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L113-L155","display_code":"function candidate() {\n    var resolvedPath = '';\n    var resolvedAbsolute = false;\n    var cwd;\n\n    for (var i = arguments.length - 1; i >= -1 && !resolvedAbsolute; i--) {\n      var path;\n      if (i >= 0)\n        path = arguments[i];\n      else {\n        if (cwd === undefined)\n          cwd = process.cwd();\n        path = cwd;\n      }\n\n      assertPath(path);\n\n      \n      if (path.length === 0) {\n        continue;\n      }\n\n      resolvedPath = path + '/' + resolvedPath;\n      resolvedAbsolute = path.charCodeAt(0) === 47 ;\n    }\n\n    \n    \n\n    \n    resolvedPath = normalizeStringPosix(resolvedPath, !resolvedAbsolute);\n\n    if (resolvedAbsolute) {\n      if (resolvedPath.length > 0)\n        return '/' + resolvedPath;\n      else\n        return '/';\n    } else if (resolvedPath.length > 0) {\n      return resolvedPath;\n    } else {\n      return '.';\n    }\n  }","normalized_hash":"sha256-447956aeaeaf09059607d7fab3de47f2c33b581a0a17df5f6a5a3460c46b6906"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:35:49.200Z","content":{"summary":"Resolves POSIX-style path segments into a normalized path using the current working directory when needed.","signature_intent":"Accepts zero or more path strings and returns a normalized path string.","behavior":["Validates each provided segment as a path string.","Ignores empty path segments.","Combines segments so later absolute input determines the base.","Uses the current working directory when the provided segments do not establish an absolute base.","Normalizes redundant separators, current-directory markers, and parent-directory markers.","Returns an absolute path when an absolute base is available.","Returns a relative normalized path when no absolute base is available."],"edge_cases":["With no arguments, returns the normalized current working directory.","With only empty segments, resolves from the current working directory.","Returns '/' when normalization resolves to the filesystem root.","Returns '.' when resolution produces no path content and no absolute base exists.","Throws for invalid path inputs."],"forbidden_details":["Internal names are intentionally omitted.","Repository and source-location details are intentionally omitted.","Low-level implementation steps are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:59:11.213Z","display_code":"function candidate(...segments) {\n  for (const segment of segments) {\n    if (typeof segment !== \"string\") {\n      throw new TypeError(\"Path must be a string\");\n    }\n  }\n\n  let hasAbsoluteBase = false;\n  const collected = [];\n\n  for (const segment of segments) {\n    if (segment === \"\") continue;\n\n    if (segment[0] === \"/\") {\n      collected.length = 0;\n      hasAbsoluteBase = true;\n    }\n\n    collected.push(segment);\n  }\n\n  if (!hasAbsoluteBase) {\n    let cwd = \"\";\n    if (typeof process !== \"undefined\" && process && typeof process.cwd === \"function\") {\n      cwd = process.cwd();\n    }\n\n    if (typeof cwd !== \"string\") {\n      throw new TypeError(\"Current working directory must be a string\");\n    }\n\n    if (cwd !== \"\") {\n      if (cwd[0] === \"/\") {\n        hasAbsoluteBase = true;\n      }\n      collected.unshift(cwd);\n    }\n  }\n\n  const stack = [];\n  const raw = collected.join(\"/\").split(\"/\");\n\n  for (const item of raw) {\n    if (item === \"\" || item === \".\") continue;\n\n    if (item === \"..\") {\n      if (stack.length > 0 && stack[stack.length - 1] !== \"..\") {\n        stack.pop();\n      } else if (!hasAbsoluteBase) {\n        stack.push(\"..\");\n      }\n    } else {\n      stack.push(item);\n    }\n  }\n\n  if (hasAbsoluteBase) {\n    return stack.length === 0 ? \"/\" : \"/\" + stack.join(\"/\");\n  }\n\n  return stack.length === 0 ? \".\" : stack.join(\"/\");\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0019","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":157,"source_end_line":173,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L157-L173","display_code":"function candidate(path) {\n    assertPath(path);\n\n    if (path.length === 0) return '.';\n\n    var isAbsolute = path.charCodeAt(0) === 47 ;\n    var trailingSeparator = path.charCodeAt(path.length - 1) === 47 ;\n\n    \n    path = normalizeStringPosix(path, !isAbsolute);\n\n    if (path.length === 0 && !isAbsolute) path = '.';\n    if (path.length > 0 && trailingSeparator) path += '/';\n\n    if (isAbsolute) return '/' + path;\n    return path;\n  }","normalized_hash":"sha256-d1003f4fb32403f5ae3b8b6c9ce060cacd30a12a55f598456b85c98ef7d53360"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:36:01.389Z","content":{"summary":"Normalizes a POSIX-style filesystem string while preserving whether it is absolute and whether it ends with a separator.","signature_intent":"Accepts one filesystem-style string and returns its normalized POSIX-style form.","behavior":["Validates that the input is an acceptable filesystem string.","Returns the current-directory marker for an empty relative input.","Preserves a leading root separator for absolute inputs.","Simplifies redundant separators and dot segments according to POSIX normalization rules.","Resolves parent-directory segments where normalization permits it.","Preserves a trailing separator when the original non-empty input ended with one.","Returns a relative normalized result for relative inputs and an absolute normalized result for absolute inputs."],"edge_cases":["Empty input returns the current-directory marker.","A relative input that normalizes to empty returns the current-directory marker.","An absolute input that normalizes to empty returns the root separator.","A non-empty input ending in a separator returns a normalized result that also ends in a separator."],"forbidden_details":["Internal helper names, local identifiers, source locations, and character-code implementation details are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:59:29.207Z","display_code":"function candidate(input) {\n  if (typeof input !== \"string\") {\n    throw new TypeError(\"Expected a string\");\n  }\n  if (input.indexOf(\"\\0\") !== -1) {\n    throw new TypeError(\"Path must not contain null bytes\");\n  }\n\n  const absolute = input.charAt(0) === \"/\";\n  const keepSlash = input.length > 0 && input.charAt(input.length - 1) === \"/\";\n  const parts = input.split(\"/\");\n  const output = [];\n\n  for (const part of parts) {\n    if (part === \"\" || part === \".\") {\n      continue;\n    }\n\n    if (part === \"..\") {\n      if (absolute) {\n        if (output.length > 0) {\n          output.pop();\n        }\n      } else if (output.length > 0 && output[output.length - 1] !== \"..\") {\n        output.pop();\n      } else {\n        output.push(part);\n      }\n    } else {\n      output.push(part);\n    }\n  }\n\n  let normalized = output.join(\"/\");\n\n  if (absolute) {\n    normalized = \"/\" + normalized;\n  }\n\n  if (normalized === \"\") {\n    normalized = absolute ? \"/\" : \".\";\n  }\n\n  if (keepSlash && normalized !== \"/\") {\n    normalized += \"/\";\n  }\n\n  return normalized;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0020","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":180,"source_end_line":197,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L180-L197","display_code":"function candidate() {\n    if (arguments.length === 0)\n      return '.';\n    var joined;\n    for (var i = 0; i < arguments.length; ++i) {\n      var arg = arguments[i];\n      assertPath(arg);\n      if (arg.length > 0) {\n        if (joined === undefined)\n          joined = arg;\n        else\n          joined += '/' + arg;\n      }\n    }\n    if (joined === undefined)\n      return '.';\n    return posix.normalize(joined);\n  }","normalized_hash":"sha256-9adcd450dab224c2dcba121aa028470c9b1291119b0404fef6bde587b99c9a74"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:36:28.381Z","content":{"summary":"Combines zero or more path segments into a normalized POSIX-style path string.","signature_intent":"Accepts any number of path segment inputs and returns a string path.","behavior":["Validates each provided segment before combining.","Ignores empty segments when building the result.","Separates retained segments with forward slashes.","Normalizes the combined path before returning it."],"edge_cases":["Returns \".\" when no segments are provided.","Returns \".\" when all provided segments are empty.","Throws if any provided segment fails path validation."],"forbidden_details":["Does not expose implementation variable names.","Does not reference repository names or source locations.","Does not describe internal control-flow details."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T14:59:47.649Z","display_code":"function candidate(...segments) {\n  for (const part of segments) {\n    if (typeof part !== \"string\") {\n      throw new TypeError(\"Path segment must be a string\");\n    }\n  }\n\n  const kept = segments.filter((part) => part.length > 0);\n  if (kept.length === 0) {\n    return \".\";\n  }\n\n  const input = kept.join(\"/\");\n  const absolute = input.startsWith(\"/\");\n  const trailing = input.endsWith(\"/\");\n  const stack = [];\n\n  for (const piece of input.split(\"/\")) {\n    if (piece === \"\" || piece === \".\") {\n      continue;\n    }\n    if (piece === \"..\") {\n      if (stack.length > 0 && stack[stack.length - 1] !== \"..\") {\n        stack.pop();\n      } else if (!absolute) {\n        stack.push(\"..\");\n      }\n    } else {\n      stack.push(piece);\n    }\n  }\n\n  let output = stack.join(\"/\");\n  if (absolute) {\n    output = \"/\" + output;\n  }\n  if (output === \"\") {\n    output = absolute ? \"/\" : \".\";\n  }\n  if (trailing && output !== \"/\" && output !== \".\") {\n    output += \"/\";\n  }\n  return output;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0021","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":199,"source_end_line":287,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L199-L287","display_code":"function candidate(from, to) {\n    assertPath(from);\n    assertPath(to);\n\n    if (from === to) return '';\n\n    from = posix.resolve(from);\n    to = posix.resolve(to);\n\n    if (from === to) return '';\n\n    \n    var fromStart = 1;\n    for (; fromStart < from.length; ++fromStart) {\n      if (from.charCodeAt(fromStart) !== 47 )\n        break;\n    }\n    var fromEnd = from.length;\n    var fromLen = fromEnd - fromStart;\n\n    \n    var toStart = 1;\n    for (; toStart < to.length; ++toStart) {\n      if (to.charCodeAt(toStart) !== 47 )\n        break;\n    }\n    var toEnd = to.length;\n    var toLen = toEnd - toStart;\n\n    \n    var length = fromLen < toLen ? fromLen : toLen;\n    var lastCommonSep = -1;\n    var i = 0;\n    for (; i <= length; ++i) {\n      if (i === length) {\n        if (toLen > length) {\n          if (to.charCodeAt(toStart + i) === 47 ) {\n            \n            \n            return to.slice(toStart + i + 1);\n          } else if (i === 0) {\n            \n            \n            return to.slice(toStart + i);\n          }\n        } else if (fromLen > length) {\n          if (from.charCodeAt(fromStart + i) === 47 ) {\n            \n            \n            lastCommonSep = i;\n          } else if (i === 0) {\n            \n            \n            lastCommonSep = 0;\n          }\n        }\n        break;\n      }\n      var fromCode = from.charCodeAt(fromStart + i);\n      var toCode = to.charCodeAt(toStart + i);\n      if (fromCode !== toCode)\n        break;\n      else if (fromCode === 47 )\n        lastCommonSep = i;\n    }\n\n    var out = '';\n    \n    \n    for (i = fromStart + lastCommonSep + 1; i <= fromEnd; ++i) {\n      if (i === fromEnd || from.charCodeAt(i) === 47 ) {\n        if (out.length === 0)\n          out += '..';\n        else\n          out += '/..';\n      }\n    }\n\n    \n    \n    if (out.length > 0)\n      return out + to.slice(toStart + lastCommonSep);\n    else {\n      toStart += lastCommonSep;\n      if (to.charCodeAt(toStart) === 47 )\n        ++toStart;\n      return to.slice(toStart);\n    }\n  }","normalized_hash":"sha256-6e4a91accff4184fb99680632c04934dfac11ce547b603888d456195b299995d"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:36:37.683Z","content":{"summary":"Computes the POSIX-style relative path needed to navigate from one supplied path to another.","signature_intent":"Accepts two path strings and returns a string representing the relative POSIX path between them.","behavior":["Validates that both inputs are paths before processing.","Returns an empty string when both paths identify the same location.","Resolves both inputs to absolute POSIX paths before comparison.","Returns the destination suffix when the origin is an ancestor of the destination.","Returns one or more parent-directory segments when navigation must move upward.","Combines upward navigation with the remaining destination path when the paths diverge.","Uses forward slashes as path separators."],"edge_cases":["Equivalent paths after resolution return an empty string.","Identical input strings return an empty string before resolution.","Paths with shared prefixes but different path segments are treated as divergent paths.","Root-relative navigation may return only the remaining destination path or parent-directory traversal as appropriate."],"forbidden_details":["Do not mention internal variable names.","Do not mention repository names or source paths.","Do not describe low-level character scanning or index bookkeeping."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:00:09.740Z","display_code":"function candidate(from, to) {\n  if (typeof from !== \"string\" || typeof to !== \"string\") {\n    throw new TypeError(\"Path arguments must be strings\");\n  }\n  if (from === to) {\n    return \"\";\n  }\n\n  function normalizeAbsolute(input) {\n    var base = \"/\";\n    if (input.charAt(0) !== \"/\" && typeof process !== \"undefined\" && process && typeof process.cwd === \"function\") {\n      base = process.cwd() || \"/\";\n    }\n\n    var combined = input.charAt(0) === \"/\" ? input : base + \"/\" + input;\n    var pieces = combined.split(\"/\");\n    var stack = [];\n\n    for (var i = 0; i < pieces.length; i++) {\n      var part = pieces[i];\n      if (!part || part === \".\") {\n        continue;\n      }\n      if (part === \"..\") {\n        if (stack.length) {\n          stack.pop();\n        }\n      } else {\n        stack.push(part);\n      }\n    }\n\n    return \"/\" + stack.join(\"/\");\n  }\n\n  var start = normalizeAbsolute(from);\n  var end = normalizeAbsolute(to);\n\n  if (start === end) {\n    return \"\";\n  }\n\n  var startParts = start === \"/\" ? [] : start.slice(1).split(\"/\");\n  var endParts = end === \"/\" ? [] : end.slice(1).split(\"/\");\n  var shared = 0;\n\n  while (shared < startParts.length && shared < endParts.length && startParts[shared] === endParts[shared]) {\n    shared++;\n  }\n\n  var result = [];\n  for (var up = shared; up < startParts.length; up++) {\n    result.push(\"..\");\n  }\n  for (var down = shared; down < endParts.length; down++) {\n    result.push(endParts[down]);\n  }\n\n  return result.join(\"/\");\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0022","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":293,"source_end_line":316,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L293-L316","display_code":"function candidate(path) {\n    assertPath(path);\n    if (path.length === 0) return '.';\n    var code = path.charCodeAt(0);\n    var hasRoot = code === 47 ;\n    var end = -1;\n    var matchedSlash = true;\n    for (var i = path.length - 1; i >= 1; --i) {\n      code = path.charCodeAt(i);\n      if (code === 47 ) {\n          if (!matchedSlash) {\n            end = i;\n            break;\n          }\n        } else {\n        \n        matchedSlash = false;\n      }\n    }\n\n    if (end === -1) return hasRoot ? '/' : '.';\n    if (hasRoot && end === 1) return '\n    return path.slice(0, end);\n  }","normalized_hash":"sha256-16260c3c9dee9a4bf043cb3ceea34db23ef0cdd898337b5eec7bcdce3c6aa7e4"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:36:53.106Z","content":{"summary":"Returns the POSIX-style parent directory for a slash-delimited filesystem location after validating the input.","signature_intent":"Accepts a single location string and returns a string naming its parent directory.","behavior":["Validates the input before computing a result.","Treats forward slash as the directory separator.","Removes the final non-root segment and any trailing separators from the result boundary.","Returns the leading directory portion when a parent directory exists.","Preserves a double leading separator when it is the parent of the first segment."],"edge_cases":["Empty input returns \".\".","A relative single-segment input returns \".\".","A root-only input returns \"/\".","Inputs with no parent directory return \".\" unless rooted.","Rooted single-segment inputs return \"/\".","Double-rooted single-segment inputs return \"//\"."],"forbidden_details":["Identifier names omitted.","Repository details omitted.","Source locations omitted.","Incidental loop mechanics omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:00:26.905Z","display_code":"function candidate(location) {\n  if (typeof location !== \"string\") {\n    throw new TypeError(\"Path must be a string\");\n  }\n\n  if (location.length === 0) {\n    return \".\";\n  }\n\n  const startsAtRoot = location.charCodeAt(0) === 47;\n  let cut = -1;\n  let skippingSeparators = true;\n\n  for (let index = location.length - 1; index >= 1; index -= 1) {\n    if (location.charCodeAt(index) === 47) {\n      if (!skippingSeparators) {\n        cut = index;\n        break;\n      }\n    } else {\n      skippingSeparators = false;\n    }\n  }\n\n  if (cut === -1) {\n    return startsAtRoot ? \"/\" : \".\";\n  }\n\n  if (startsAtRoot && cut === 1) {\n    return \"//\";\n  }\n\n  return location.slice(0, cut);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0023","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":318,"source_end_line":387,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L318-L387","display_code":"function candidate(path, ext) {\n    if (ext !== undefined && typeof ext !== 'string') throw new TypeError('\"ext\" argument must be a string');\n    assertPath(path);\n\n    var start = 0;\n    var end = -1;\n    var matchedSlash = true;\n    var i;\n\n    if (ext !== undefined && ext.length > 0 && ext.length <= path.length) {\n      if (ext.length === path.length && ext === path) return '';\n      var extIdx = ext.length - 1;\n      var firstNonSlashEnd = -1;\n      for (i = path.length - 1; i >= 0; --i) {\n        var code = path.charCodeAt(i);\n        if (code === 47 ) {\n            \n            \n            if (!matchedSlash) {\n              start = i + 1;\n              break;\n            }\n          } else {\n          if (firstNonSlashEnd === -1) {\n            \n            \n            matchedSlash = false;\n            firstNonSlashEnd = i + 1;\n          }\n          if (extIdx >= 0) {\n            \n            if (code === ext.charCodeAt(extIdx)) {\n              if (--extIdx === -1) {\n                \n                \n                end = i;\n              }\n            } else {\n              \n              \n              extIdx = -1;\n              end = firstNonSlashEnd;\n            }\n          }\n        }\n      }\n\n      if (start === end) end = firstNonSlashEnd;else if (end === -1) end = path.length;\n      return path.slice(start, end);\n    } else {\n      for (i = path.length - 1; i >= 0; --i) {\n        if (path.charCodeAt(i) === 47 ) {\n            \n            \n            if (!matchedSlash) {\n              start = i + 1;\n              break;\n            }\n          } else if (end === -1) {\n          \n          \n          matchedSlash = false;\n          end = i + 1;\n        }\n      }\n\n      if (end === -1) return '';\n      return path.slice(start, end);\n    }\n  }","normalized_hash":"sha256-cb834cb40f2486baec7035804ea8496f5e140194dc801ee439c22ca75d9c953f"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:37:04.665Z","content":{"summary":"Returns the final non-empty POSIX-style path segment, optionally removing a matching trailing extension.","signature_intent":"Accepts a path-like string as the first input and an optional string extension as the second input; returns a string.","behavior":["Validates the first input as a path string using the host path validation rules.","Throws TypeError when the second input is provided and is not a string.","Ignores trailing slash characters when identifying the final segment.","Returns the final segment after the last slash character.","Removes the supplied extension only when it matches the end of the final segment.","Leaves the final segment unchanged when the supplied extension does not match."],"edge_cases":["Returns an empty string when no non-slash segment exists.","Returns an empty string when the supplied extension exactly equals the entire path string.","Does not remove an empty extension.","Does not remove an extension longer than the path string.","If extension removal would consume the whole final segment, returns the full final segment instead."],"forbidden_details":["Internal variable names.","Repository names.","Source paths.","Loop mechanics or incidental implementation quirks."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:00:44.093Z","display_code":"function candidate(input, extension) {\n  if (typeof input !== 'string') {\n    throw new TypeError('The path argument must be of type string. Received ' + typeof input);\n  }\n  if (arguments.length > 1 && typeof extension !== 'string') {\n    throw new TypeError('The ext argument must be of type string. Received ' + typeof extension);\n  }\n\n  if (arguments.length > 1 && extension === input) {\n    return '';\n  }\n\n  let end = input.length - 1;\n  while (end >= 0 && input.charCodeAt(end) === 47) {\n    end--;\n  }\n  if (end < 0) {\n    return '';\n  }\n\n  let start = end;\n  while (start >= 0 && input.charCodeAt(start) !== 47) {\n    start--;\n  }\n\n  const segment = input.slice(start + 1, end + 1);\n  if (\n    arguments.length > 1 &&\n    extension.length > 0 &&\n    extension.length < segment.length &&\n    segment.endsWith(extension)\n  ) {\n    return segment.slice(0, segment.length - extension.length);\n  }\n\n  return segment;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0024","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":389,"source_end_line":436,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L389-L436","display_code":"function candidate(path) {\n    assertPath(path);\n    var startDot = -1;\n    var startPart = 0;\n    var end = -1;\n    var matchedSlash = true;\n    \n    \n    var preDotState = 0;\n    for (var i = path.length - 1; i >= 0; --i) {\n      var code = path.charCodeAt(i);\n      if (code === 47 ) {\n          \n          \n          if (!matchedSlash) {\n            startPart = i + 1;\n            break;\n          }\n          continue;\n        }\n      if (end === -1) {\n        \n        \n        matchedSlash = false;\n        end = i + 1;\n      }\n      if (code === 46 ) {\n          \n          if (startDot === -1)\n            startDot = i;\n          else if (preDotState !== 1)\n            preDotState = 1;\n      } else if (startDot !== -1) {\n        \n        \n        preDotState = -1;\n      }\n    }\n\n    if (startDot === -1 || end === -1 ||\n        \n        preDotState === 0 ||\n        \n        preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {\n      return '';\n    }\n    return path.slice(startDot, end);\n  }","normalized_hash":"sha256-90bf613d85d0c87f74377af6f8089b0b411cabbe4befe65625f595927b355634"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:37:23.339Z","content":{"summary":"Returns the file extension from the final path segment, including its leading dot, after validating the input as a path string.","signature_intent":"Accepts a path-like string and returns a string extension or an empty string when no extension is present.","behavior":["Validates the input before processing.","Considers only the final path segment.","Ignores trailing path separators.","Returns the substring from the last extension dot through the end of the final segment.","Includes the leading dot in the returned extension.","Returns an empty string when the final segment has no extension."],"edge_cases":["Returns an empty string for an empty path or a path containing only separators.","Returns an empty string for hidden-style names with no additional extension.","Returns an empty string for single-dot or double-dot final segments.","Returns an empty string when the only dot does not indicate an extension."],"forbidden_details":["Variable names","Repository names","Source paths","Unnecessary internal scanning details"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:01:00.599Z","display_code":"function candidate(path) {\n  if (typeof path !== \"string\") {\n    throw new TypeError(\"Path must be a string\");\n  }\n\n  let end = path.length;\n  while (end > 0 && (path[end - 1] === \"/\" || path[end - 1] === \"\\\\\")) {\n    end--;\n  }\n\n  if (end === 0) {\n    return \"\";\n  }\n\n  const slash = path.lastIndexOf(\"/\", end - 1);\n  const backslash = path.lastIndexOf(\"\\\\\", end - 1);\n  const start = Math.max(slash, backslash) + 1;\n  const name = path.slice(start, end);\n\n  if (name === \".\" || name === \"..\") {\n    return \"\";\n  }\n\n  const dot = name.lastIndexOf(\".\");\n  if (dot <= 0 || dot === name.length - 1) {\n    return \"\";\n  }\n\n  return name.slice(dot);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0025","language":"js","review_status":"approved","license":"MIT","human":{"source_repo":"browserify/path-browserify","source_commit":"872fec31a8bac7b9b43be0e54ef3037e0202c5fb","source_path":"index.js","source_start_line":445,"source_end_line":519,"source_url":"https://github.com/browserify/path-browserify/blob/872fec31a8bac7b9b43be0e54ef3037e0202c5fb/index.js#L445-L519","display_code":"function candidate(path) {\n    assertPath(path);\n\n    var ret = { root: '', dir: '', base: '', ext: '', name: '' };\n    if (path.length === 0) return ret;\n    var code = path.charCodeAt(0);\n    var isAbsolute = code === 47 ;\n    var start;\n    if (isAbsolute) {\n      ret.root = '/';\n      start = 1;\n    } else {\n      start = 0;\n    }\n    var startDot = -1;\n    var startPart = 0;\n    var end = -1;\n    var matchedSlash = true;\n    var i = path.length - 1;\n\n    \n    \n    var preDotState = 0;\n\n    \n    for (; i >= start; --i) {\n      code = path.charCodeAt(i);\n      if (code === 47 ) {\n          \n          \n          if (!matchedSlash) {\n            startPart = i + 1;\n            break;\n          }\n          continue;\n        }\n      if (end === -1) {\n        \n        \n        matchedSlash = false;\n        end = i + 1;\n      }\n      if (code === 46 ) {\n          \n          if (startDot === -1) startDot = i;else if (preDotState !== 1) preDotState = 1;\n        } else if (startDot !== -1) {\n        \n        \n        preDotState = -1;\n      }\n    }\n\n    if (startDot === -1 || end === -1 ||\n    \n    preDotState === 0 ||\n    \n    preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {\n      if (end !== -1) {\n        if (startPart === 0 && isAbsolute) ret.base = ret.name = path.slice(1, end);else ret.base = ret.name = path.slice(startPart, end);\n      }\n    } else {\n      if (startPart === 0 && isAbsolute) {\n        ret.name = path.slice(1, startDot);\n        ret.base = path.slice(1, end);\n      } else {\n        ret.name = path.slice(startPart, startDot);\n        ret.base = path.slice(startPart, end);\n      }\n      ret.ext = path.slice(startDot, end);\n    }\n\n    if (startPart > 0) ret.dir = path.slice(0, startPart - 1);else if (isAbsolute) ret.dir = '/';\n\n    return ret;\n  }","normalized_hash":"sha256-58e931bab6371d010dbe4eb560abf32930831122a7b139ea76ba3ca1b61cbdeb"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:37:40.019Z","content":{"summary":"Parses a POSIX-style filesystem string into root, directory, filename, extension, and stem components.","signature_intent":"Accepts a validated string and returns an object with string fields: root, dir, base, ext, and name.","behavior":["Recognizes only forward slash as a directory separator.","Treats a leading forward slash as an absolute root.","Ignores trailing separators when determining the final file component.","Sets base to the final non-separator component.","Sets name to base without the final extension when an extension is present.","Sets ext to the final dot segment of the final component when it qualifies as an extension.","Sets dir to the containing directory, excluding the final separator.","Returns empty strings for components that are not present."],"edge_cases":["Empty input returns all fields as empty strings.","Root-only input returns root and dir as slash with empty base, ext, and name.","Inputs ending in separators are parsed as if those trailing separators were absent.","A final component of only dots does not produce an extension.","A hidden-name style final component such as a single leading dot without another stem character does not produce an extension.","Relative inputs with no directory leave root and dir empty."],"forbidden_details":[]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:01:19.226Z","display_code":"function candidate(path) {\n  let limit = path.length;\n  while (limit > 0 && path[limit - 1] === \"/\") limit--;\n\n  const root = path[0] === \"/\" ? \"/\" : \"\";\n\n  if (limit === 0) {\n    return { root, dir: root, base: \"\", ext: \"\", name: \"\" };\n  }\n\n  let split = -1;\n  for (let i = limit - 1; i >= 0; i--) {\n    if (path[i] === \"/\") {\n      split = i;\n      break;\n    }\n  }\n\n  const base = path.slice(split + 1, limit);\n  let dir = \"\";\n  if (split === 0 && root) dir = \"/\";\n  else if (split > 0) dir = path.slice(0, split);\n\n  let dot = -1;\n  for (let i = base.length - 1; i >= 0; i--) {\n    if (base[i] === \".\") {\n      dot = i;\n      break;\n    }\n  }\n\n  let ext = \"\";\n  let name = base;\n  if (dot > 0 && !/^\\.+$/.test(base)) {\n    ext = base.slice(dot);\n    name = base.slice(0, dot);\n  }\n\n  return { root, dir, base, ext, name };\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0026","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/parse.js","source_start_line":50,"source_end_line":112,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/parse.js#L50-L112","display_code":"function candidate(str, options) {\n    var obj = {};\n    var cleanStr = options.ignoreQueryPrefix ? str.replace(/^\\?/, '') : str;\n    var limit = options.parameterLimit === Infinity ? undefined : options.parameterLimit;\n    var parts = cleanStr.split(options.delimiter, limit);\n    var skipIndex = -1; \n    var i;\n\n    var charset = options.charset;\n    if (options.charsetSentinel) {\n        for (i = 0; i < parts.length; ++i) {\n            if (parts[i].indexOf('utf8=') === 0) {\n                if (parts[i] === charsetSentinel) {\n                    charset = 'utf-8';\n                } else if (parts[i] === isoSentinel) {\n                    charset = 'iso-8859-1';\n                }\n                skipIndex = i;\n                i = parts.length; \n            }\n        }\n    }\n\n    for (i = 0; i < parts.length; ++i) {\n        if (i === skipIndex) {\n            continue;\n        }\n        var part = parts[i];\n\n        var bracketEqualsPos = part.indexOf(']=');\n        var pos = bracketEqualsPos === -1 ? part.indexOf('=') : bracketEqualsPos + 1;\n\n        var key, val;\n        if (pos === -1) {\n            key = options.decoder(part, defaults.decoder, charset, 'key');\n            val = options.strictNullHandling ? null : '';\n        } else {\n            key = options.decoder(part.slice(0, pos), defaults.decoder, charset, 'key');\n            val = utils.maybeMap(\n                parseArrayValue(part.slice(pos + 1), options),\n                function (encodedVal) {\n                    return options.decoder(encodedVal, defaults.decoder, charset, 'value');\n                }\n            );\n        }\n\n        if (val && options.interpretNumericEntities && charset === 'iso-8859-1') {\n            val = interpretNumericEntities(val);\n        }\n\n        if (part.indexOf('[]=') > -1) {\n            val = isArray(val) ? [val] : val;\n        }\n\n        if (has.call(obj, key)) {\n            obj[key] = utils.combine(obj[key], val);\n        } else {\n            obj[key] = val;\n        }\n    }\n\n    return obj;\n}","normalized_hash":"sha256-da1a8c6a78fa931dbe4f4a96b06e152fa5348d759616149fec64ef9f0be726e2"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:37:52.525Z","content":{"summary":"Parses an encoded parameter string into a key-value mapping according to configurable delimiter, decoding, charset, null, and array-handling rules.","signature_intent":"Input is a string plus parsing settings; output is an object mapping decoded keys to decoded values.","behavior":["Optionally removes a leading question mark before parsing.","Splits the input using the configured delimiter and optional maximum parameter count.","Can detect an encoding marker and use it to choose between UTF-8 and ISO-8859-1 decoding.","Skips the encoding marker entry when producing output.","Decodes each key and value with the selected charset.","Treats entries without an equals sign as null when strict null handling is enabled, otherwise as an empty string.","Combines repeated keys into aggregated values.","Interprets numeric character entities for ISO-8859-1 values when enabled.","Preserves explicit array-style assignments according to the configured array parsing behavior."],"edge_cases":["An unlimited parameter count parses all entries.","A missing value separator produces a key with a null or empty-string value.","Repeated keys do not overwrite earlier values.","The encoding marker affects only entries parsed after detection.","Array-style syntax can wrap parsed array values to preserve nesting."],"forbidden_details":["Internal variable names","Repository names","Source paths","Implementation-only loop or index details","Unnecessary algorithmic quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:01:50.384Z","display_code":"function candidate(input, options) {\n  const config = options || {};\n  const delimiter = config.delimiter === undefined ? '&' : config.delimiter;\n  const limit = config.parameterLimit === undefined ? 1000 : config.parameterLimit;\n  const strictNull = config.strictNullHandling === true;\n  const useSentinel = config.charsetSentinel === true;\n  const numericEntities = config.interpretNumericEntities === true;\n  const commaArrays = config.comma === true;\n  const parseArrays = config.parseArrays !== false;\n  let charset = config.charset === 'iso-8859-1' ? 'iso-8859-1' : 'utf-8';\n  let text = String(input == null ? '' : input);\n\n  if (config.ignoreQueryPrefix) text = text.replace(/^\\?/, '');\n  if (text.length === 0) return {};\n\n  const pieces = limit === Infinity ? text.split(delimiter) : text.split(delimiter, limit);\n  const result = {};\n\n  function decode(part) {\n    const raw = String(part).replace(/\\+/g, ' ');\n    if (typeof config.decoder === 'function') {\n      return config.decoder(raw, decodeURIComponent, charset, 'value');\n    }\n    if (charset === 'iso-8859-1') {\n      let out = raw.replace(/%([0-9a-f]{2})/gi, function (_, hex) {\n        return String.fromCharCode(parseInt(hex, 16));\n      });\n      return numericEntities ? out.replace(/&#(\\d+);/g, function (_, code) {\n        return String.fromCharCode(parseInt(code, 10));\n      }) : out;\n    }\n    try {\n      return decodeURIComponent(raw);\n    } catch (_) {\n      return raw;\n    }\n  }\n\n  function assign(target, key, value) {\n    if (Object.prototype.hasOwnProperty.call(target, key)) {\n      target[key] = Array.isArray(target[key]) ? target[key].concat(value) : [target[key]].concat(value);\n    } else {\n      target[key] = value;\n    }\n  }\n\n  function build(key, value) {\n    if (!parseArrays) return { key, value };\n    const match = /^(.*)\\[\\]$/.exec(key);\n    if (!match) return { key, value };\n    return { key: match[1], value: Array.isArray(value) ? [value] : [value] };\n  }\n\n  for (const item of pieces) {\n    if (item === '') continue;\n    const eq = item.indexOf('=');\n    let rawKey = eq < 0 ? item : item.slice(0, eq);\n    let rawValue = eq < 0 ? (strictNull ? null : '') : item.slice(eq + 1);\n\n    if (useSentinel && rawKey === 'utf8') {\n      if (rawValue === '%E2%9C%93' || rawValue === '✓') charset = 'utf-8';\n      if (rawValue === '%26%2310003%3B' || rawValue === '&#10003;') charset = 'iso-8859-1';\n      continue;\n    }\n\n    const decodedKey = decode(rawKey);\n    let decodedValue;\n    if (rawValue === null) {\n      decodedValue = null;\n    } else if (commaArrays && rawValue.indexOf(',') >= 0) {\n      decodedValue = rawValue.split(',').map(decode);\n    } else {\n      decodedValue = decode(rawValue);\n    }\n\n    const entry = build(decodedKey, decodedValue);\n    assign(result, entry.key, entry.value);\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0027","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/parse.js","source_start_line":149,"source_end_line":201,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/parse.js#L149-L201","display_code":"function candidate(givenKey, val, options, valuesParsed) {\n    if (!givenKey) {\n        return;\n    }\n\n    \n    var key = options.allowDots ? givenKey.replace(/\\.([^.[]+)/g, '[$1]') : givenKey;\n\n    \n\n    var brackets = /(\\[[^[\\]]*])/;\n    var child = /(\\[[^[\\]]*])/g;\n\n    \n\n    var segment = options.depth > 0 && brackets.exec(key);\n    var parent = segment ? key.slice(0, segment.index) : key;\n\n    \n\n    var keys = [];\n    if (parent) {\n        \n        if (!options.plainObjects && has.call(Object.prototype, parent)) {\n            if (!options.allowPrototypes) {\n                return;\n            }\n        }\n\n        keys.push(parent);\n    }\n\n    \n\n    var i = 0;\n    while (options.depth > 0 && (segment = child.exec(key)) !== null && i < options.depth) {\n        i += 1;\n        if (!options.plainObjects && has.call(Object.prototype, segment[1].slice(1, -1))) {\n            if (!options.allowPrototypes) {\n                return;\n            }\n        }\n        keys.push(segment[1]);\n    }\n\n    \n\n    if (segment) {\n        keys.push('[' + key.slice(segment.index) + ']');\n    }\n\n    return parseObject(keys, val, options, valuesParsed);\n}","normalized_hash":"sha256-8fb0b32bdc3c04af174fb98015e3ad81e0393f68ddce4772e12a83b7fd8cdbcf"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:38:02.797Z","content":{"summary":"Transforms a non-empty structured property string and associated data into a nested parsed object according to parsing options.","signature_intent":"Accepts a property expression, data to assign, parser configuration, and parsed-value metadata; returns a nested object representation or no result when validation blocks parsing.","behavior":["Returns no result when the property expression is empty or otherwise falsy.","Optionally treats dot notation as nested bracket notation when configured to do so.","Extracts the top-level property and nested bracket segments up to the configured depth limit.","Rejects properties that would target built-in object prototype fields unless prototype access is explicitly allowed or plain-object mode is enabled.","Preserves any remaining unprocessed nested path beyond the depth limit as a single trailing property segment.","Delegates final nested object construction to the object parser using the collected path segments and supplied data."],"edge_cases":["A top-level property matching a protected prototype field returns no result under default protection settings.","A nested property matching a protected prototype field returns no result under default protection settings.","When depth is zero or less, bracket parsing is skipped and the full property expression is treated as the top-level property.","When the depth limit is reached before the full expression is consumed, the remaining path is grouped into one final segment.","If there is no top-level property before the first bracketed segment, only nested segments are used."],"forbidden_details":["Original variable names, repository names, source paths, and incidental implementation quirks are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:02:15.331Z","display_code":"function candidate(expression, payload, config, parsedMeta) {\n  if (!expression) {\n    return undefined;\n  }\n\n  var opts = config || {};\n  var limit = typeof opts.depth === 'number' ? opts.depth : 5;\n  var text = String(expression);\n\n  if (opts.allowDots) {\n    text = text.replace(/\\.([^.[\\]]+)/g, '[$1]');\n  }\n\n  var guarded = Object.prototype.hasOwnProperty;\n  var pieces = [];\n\n  if (limit > 0) {\n    var firstBracket = text.search(/\\[[^[\\]]*]/);\n    var root = firstBracket >= 0 ? text.slice(0, firstBracket) : text;\n\n    if (root) {\n      if (!opts.plainObjects && guarded.call(Object.prototype, root) && !opts.allowPrototypes) {\n        return undefined;\n      }\n      pieces.push(root);\n    }\n\n    var matcher = /\\[[^[\\]]*]/g;\n    var match;\n    var count = 0;\n\n    while ((match = matcher.exec(text)) !== null && count < limit) {\n      var inner = match[0].slice(1, -1);\n      if (!opts.plainObjects && guarded.call(Object.prototype, inner) && !opts.allowPrototypes) {\n        return undefined;\n      }\n      pieces.push(match[0]);\n      count += 1;\n    }\n\n    if (match) {\n      pieces.push('[' + text.slice(match.index) + ']');\n    }\n  } else {\n    if (!opts.plainObjects && guarded.call(Object.prototype, text) && !opts.allowPrototypes) {\n      return undefined;\n    }\n    pieces.push(text);\n  }\n\n  var build = typeof opts.parseObject === 'function' ? opts.parseObject : candidate.parseObject;\n  return build(pieces, payload, opts, parsedMeta);\n}\n\ncandidate.parseObject = function (segments, value, options, metadata) {\n  var opts = options || {};\n  var leaf = value;\n\n  for (var i = segments.length - 1; i >= 0; i -= 1) {\n    var raw = segments[i];\n    var key = raw;\n    if (typeof key === 'string' && key.charAt(0) === '[' && key.charAt(key.length - 1) === ']') {\n      key = key.slice(1, -1);\n    }\n\n    var container;\n    if (key === '') {\n      container = [];\n      container.push(leaf);\n    } else if (!opts.parseArrays || !/^\\d+$/.test(key)) {\n      container = opts.plainObjects ? Object.create(null) : {};\n      container[key] = leaf;\n    } else {\n      container = [];\n      container[Number(key)] = leaf;\n    }\n    leaf = container;\n  }\n\n  return leaf;\n};","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0028","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/parse.js","source_start_line":203,"source_end_line":235,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/parse.js#L203-L235","display_code":"function candidate(opts) {\n    if (!opts) {\n        return defaults;\n    }\n\n    if (opts.decoder !== null && opts.decoder !== undefined && typeof opts.decoder !== 'function') {\n        throw new TypeError('Decoder has to be a function.');\n    }\n\n    if (typeof opts.charset !== 'undefined' && opts.charset !== 'utf-8' && opts.charset !== 'iso-8859-1') {\n        throw new TypeError('The charset option must be either utf-8, iso-8859-1, or undefined');\n    }\n    var charset = typeof opts.charset === 'undefined' ? defaults.charset : opts.charset;\n\n    return {\n        allowDots: typeof opts.allowDots === 'undefined' ? defaults.allowDots : !!opts.allowDots,\n        allowPrototypes: typeof opts.allowPrototypes === 'boolean' ? opts.allowPrototypes : defaults.allowPrototypes,\n        arrayLimit: typeof opts.arrayLimit === 'number' ? opts.arrayLimit : defaults.arrayLimit,\n        charset: charset,\n        charsetSentinel: typeof opts.charsetSentinel === 'boolean' ? opts.charsetSentinel : defaults.charsetSentinel,\n        comma: typeof opts.comma === 'boolean' ? opts.comma : defaults.comma,\n        decoder: typeof opts.decoder === 'function' ? opts.decoder : defaults.decoder,\n        delimiter: typeof opts.delimiter === 'string' || utils.isRegExp(opts.delimiter) ? opts.delimiter : defaults.delimiter,\n        \n        depth: (typeof opts.depth === 'number' || opts.depth === false) ? +opts.depth : defaults.depth,\n        ignoreQueryPrefix: opts.ignoreQueryPrefix === true,\n        interpretNumericEntities: typeof opts.interpretNumericEntities === 'boolean' ? opts.interpretNumericEntities : defaults.interpretNumericEntities,\n        parameterLimit: typeof opts.parameterLimit === 'number' ? opts.parameterLimit : defaults.parameterLimit,\n        parseArrays: opts.parseArrays !== false,\n        plainObjects: typeof opts.plainObjects === 'boolean' ? opts.plainObjects : defaults.plainObjects,\n        strictNullHandling: typeof opts.strictNullHandling === 'boolean' ? opts.strictNullHandling : defaults.strictNullHandling\n    };\n}","normalized_hash":"sha256-7f24e0059b600f863393961ddbacdf2d213377c01417dfcb78d715426ddd2083"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:38:11.709Z","content":{"summary":"Normalizes an optional parsing configuration object into a complete options object using defaults and validating supported decoder and charset settings.","signature_intent":"Accepts an optional options object and returns either the default options object or a normalized options object for parsing behavior.","behavior":["Returns the shared default options when no options object is provided.","Rejects a non-null, non-undefined decoder value unless it is a function.","Rejects a charset value unless it is undefined, utf-8, or iso-8859-1.","Uses the default charset when charset is omitted.","Normalizes boolean-like dot support with truthiness when provided, otherwise uses the default.","Uses provided boolean values for prototype, sentinel, comma, numeric entity, plain object, and strict null handling flags; otherwise uses defaults.","Uses provided numeric limits for arrays and parameters; otherwise uses defaults.","Uses a provided decoder function; otherwise uses the default decoder.","Uses a provided string or regular-expression delimiter; otherwise uses the default delimiter.","Accepts numeric depth or false and coerces it to a number; otherwise uses the default depth.","Enables query-prefix ignoring only when explicitly true.","Enables array parsing unless explicitly false."],"edge_cases":["Null or undefined options return defaults without validation.","Null or undefined decoder values are allowed and fall back to the default decoder.","Unsupported charset values throw a TypeError.","A false depth value is accepted and becomes numeric zero.","Non-boolean values for most boolean flags fall back to defaults, except dot support is coerced by truthiness and query-prefix ignoring requires true.","Array parsing remains enabled for any value other than false."],"forbidden_details":["No variable names, repository names, source paths, or incidental implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:02:59.676Z","display_code":"const candidateDefaultOptions = {\n  allowDots: false,\n  allowPrototypes: false,\n  arrayLimit: 20,\n  charset: 'utf-8',\n  charsetSentinel: false,\n  comma: false,\n  decoder: function (value) { return value; },\n  delimiter: '&',\n  depth: 5,\n  ignoreQueryPrefix: false,\n  interpretNumericEntities: false,\n  parameterLimit: 1000,\n  parseArrays: true,\n  plainObjects: false,\n  strictNullHandling: false\n};\n\nfunction candidate(options) {\n  if (options == null) {\n    return candidateDefaultOptions;\n  }\n\n  if (options.decoder != null && typeof options.decoder !== 'function') {\n    throw new TypeError('Decoder has to be a function.');\n  }\n\n  if (options.charset !== undefined && options.charset !== 'utf-8' && options.charset !== 'iso-8859-1') {\n    throw new TypeError('The charset option must be either utf-8, iso-8859-1, or undefined');\n  }\n\n  const isPattern = Object.prototype.toString.call(options.delimiter) === '[object RegExp]';\n\n  return {\n    allowDots: options.allowDots === undefined ? candidateDefaultOptions.allowDots : !!options.allowDots,\n    allowPrototypes: typeof options.allowPrototypes === 'boolean' ? options.allowPrototypes : candidateDefaultOptions.allowPrototypes,\n    arrayLimit: typeof options.arrayLimit === 'number' ? options.arrayLimit : candidateDefaultOptions.arrayLimit,\n    charset: options.charset === undefined ? candidateDefaultOptions.charset : options.charset,\n    charsetSentinel: typeof options.charsetSentinel === 'boolean' ? options.charsetSentinel : candidateDefaultOptions.charsetSentinel,\n    comma: typeof options.comma === 'boolean' ? options.comma : candidateDefaultOptions.comma,\n    decoder: typeof options.decoder === 'function' ? options.decoder : candidateDefaultOptions.decoder,\n    delimiter: typeof options.delimiter === 'string' || isPattern ? options.delimiter : candidateDefaultOptions.delimiter,\n    depth: typeof options.depth === 'number' || options.depth === false ? +options.depth : candidateDefaultOptions.depth,\n    ignoreQueryPrefix: options.ignoreQueryPrefix === true,\n    interpretNumericEntities: typeof options.interpretNumericEntities === 'boolean' ? options.interpretNumericEntities : candidateDefaultOptions.interpretNumericEntities,\n    parameterLimit: typeof options.parameterLimit === 'number' ? options.parameterLimit : candidateDefaultOptions.parameterLimit,\n    parseArrays: options.parseArrays !== false,\n    plainObjects: typeof options.plainObjects === 'boolean' ? options.plainObjects : candidateDefaultOptions.plainObjects,\n    strictNullHandling: typeof options.strictNullHandling === 'boolean' ? options.strictNullHandling : candidateDefaultOptions.strictNullHandling\n  };\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0030","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/stringify.js","source_start_line":148,"source_end_line":192,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/stringify.js#L148-L192","display_code":"function candidate(opts) {\n    if (!opts) {\n        return defaults;\n    }\n\n    if (opts.encoder !== null && opts.encoder !== undefined && typeof opts.encoder !== 'function') {\n        throw new TypeError('Encoder has to be a function.');\n    }\n\n    var charset = opts.charset || defaults.charset;\n    if (typeof opts.charset !== 'undefined' && opts.charset !== 'utf-8' && opts.charset !== 'iso-8859-1') {\n        throw new TypeError('The charset option must be either utf-8, iso-8859-1, or undefined');\n    }\n\n    var format = formats['default'];\n    if (typeof opts.format !== 'undefined') {\n        if (!has.call(formats.formatters, opts.format)) {\n            throw new TypeError('Unknown format option provided.');\n        }\n        format = opts.format;\n    }\n    var formatter = formats.formatters[format];\n\n    var filter = defaults.filter;\n    if (typeof opts.filter === 'function' || isArray(opts.filter)) {\n        filter = opts.filter;\n    }\n\n    return {\n        addQueryPrefix: typeof opts.addQueryPrefix === 'boolean' ? opts.addQueryPrefix : defaults.addQueryPrefix,\n        allowDots: typeof opts.allowDots === 'undefined' ? defaults.allowDots : !!opts.allowDots,\n        charset: charset,\n        charsetSentinel: typeof opts.charsetSentinel === 'boolean' ? opts.charsetSentinel : defaults.charsetSentinel,\n        delimiter: typeof opts.delimiter === 'undefined' ? defaults.delimiter : opts.delimiter,\n        encode: typeof opts.encode === 'boolean' ? opts.encode : defaults.encode,\n        encoder: typeof opts.encoder === 'function' ? opts.encoder : defaults.encoder,\n        encodeValuesOnly: typeof opts.encodeValuesOnly === 'boolean' ? opts.encodeValuesOnly : defaults.encodeValuesOnly,\n        filter: filter,\n        formatter: formatter,\n        serializeDate: typeof opts.serializeDate === 'function' ? opts.serializeDate : defaults.serializeDate,\n        skipNulls: typeof opts.skipNulls === 'boolean' ? opts.skipNulls : defaults.skipNulls,\n        sort: typeof opts.sort === 'function' ? opts.sort : null,\n        strictNullHandling: typeof opts.strictNullHandling === 'boolean' ? opts.strictNullHandling : defaults.strictNullHandling\n    };\n}","normalized_hash":"sha256-908e5fdf5fa9d591d3aeaa2413b9834df70ee0ae920bfc831b5c70df2075b3c2"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:39:16.777Z","content":{"summary":"Normalizes an optional configuration object into a complete serialization configuration with validation and defaults.","signature_intent":"Accepts an optional settings object and returns either the shared default configuration or a normalized configuration object.","behavior":["Returns the default configuration when no settings object is provided.","Rejects a supplied encoding callback unless it is callable or nullish.","Uses the provided text encoding when valid, otherwise falls back to the default encoding.","Accepts only utf-8, iso-8859-1, or an omitted text encoding.","Rejects unknown output formatting choices.","Uses the selected formatting function in the returned configuration.","Uses a supplied filtering rule only when it is callable or an array.","Normalizes boolean feature flags by accepting booleans directly and otherwise applying documented fallbacks.","Uses supplied delimiter and sorting callback when valid according to their expected types.","Uses a supplied date serialization callback only when callable."],"edge_cases":["A null or undefined settings object returns defaults without further validation.","A null custom encoding callback is accepted and replaced by the default callback.","Invalid text encoding throws TypeError even if a fallback value would otherwise exist.","An unknown formatting choice throws TypeError.","Non-callable filtering values are ignored unless they are arrays.","Non-boolean flag values are coerced only for the dot-notation setting; other flags use defaults.","A non-callable sorting value becomes null."],"forbidden_details":["Local variable names omitted.","Repository and source path omitted.","Internal lookup mechanics omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:04:29.736Z","display_code":"function candidate(settings) {\n  var formatters = {\n    RFC1738: function (value) {\n      return String(value).replace(/%20/g, '+');\n    },\n    RFC3986: function (value) {\n      return String(value);\n    }\n  };\n\n  function defaultEncoder(value) {\n    return encodeURIComponent(String(value));\n  }\n\n  function defaultDateSerializer(value) {\n    return value instanceof Date ? value.toISOString() : value;\n  }\n\n  var sharedDefault = candidate.defaultConfig || (candidate.defaultConfig = {\n    encoding: defaultEncoder,\n    textEncoding: 'utf-8',\n    format: 'RFC3986',\n    formatter: formatters.RFC3986,\n    filter: null,\n    allowDots: false,\n    encodeValuesOnly: false,\n    skipNulls: false,\n    strictNullHandling: false,\n    delimiter: '&',\n    sort: null,\n    serializeDate: defaultDateSerializer\n  });\n\n  if (settings == null) {\n    return sharedDefault;\n  }\n\n  var encodingCallback = settings.encoding || settings.encoder;\n  if (encodingCallback != null && typeof encodingCallback !== 'function') {\n    throw new TypeError('Encoder must be a function.');\n  }\n\n  var requestedEncoding = settings.textEncoding || settings.charset;\n  if (requestedEncoding == null) {\n    requestedEncoding = sharedDefault.textEncoding;\n  } else {\n    requestedEncoding = String(requestedEncoding).toLowerCase();\n    if (requestedEncoding !== 'utf-8' && requestedEncoding !== 'iso-8859-1') {\n      throw new TypeError('The text encoding must be utf-8 or iso-8859-1.');\n    }\n  }\n\n  var requestedFormat = settings.format == null ? sharedDefault.format : settings.format;\n  if (!Object.prototype.hasOwnProperty.call(formatters, requestedFormat)) {\n    throw new TypeError('Unknown format option provided.');\n  }\n\n  return {\n    encoding: encodingCallback || sharedDefault.encoding,\n    textEncoding: requestedEncoding,\n    format: requestedFormat,\n    formatter: formatters[requestedFormat],\n    filter: typeof settings.filter === 'function' || Array.isArray(settings.filter) ? settings.filter : sharedDefault.filter,\n    allowDots: typeof settings.allowDots === 'boolean' ? settings.allowDots : !!settings.allowDots,\n    encodeValuesOnly: typeof settings.encodeValuesOnly === 'boolean' ? settings.encodeValuesOnly : sharedDefault.encodeValuesOnly,\n    skipNulls: typeof settings.skipNulls === 'boolean' ? settings.skipNulls : sharedDefault.skipNulls,\n    strictNullHandling: typeof settings.strictNullHandling === 'boolean' ? settings.strictNullHandling : sharedDefault.strictNullHandling,\n    delimiter: typeof settings.delimiter === 'string' ? settings.delimiter : sharedDefault.delimiter,\n    sort: typeof settings.sort === 'function' ? settings.sort : null,\n    serializeDate: typeof settings.serializeDate === 'function' ? settings.serializeDate : sharedDefault.serializeDate\n  };\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0031","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/utils.js","source_start_line":15,"source_end_line":32,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/utils.js#L15-L32","display_code":"function candidate(queue) {\n    while (queue.length > 1) {\n        var item = queue.pop();\n        var obj = item.obj[item.prop];\n\n        if (isArray(obj)) {\n            var compacted = [];\n\n            for (var j = 0; j < obj.length; ++j) {\n                if (typeof obj[j] !== 'undefined') {\n                    compacted.push(obj[j]);\n                }\n            }\n\n            item.obj[item.prop] = compacted;\n        }\n    }\n}","normalized_hash":"sha256-0cf3dcd6a9175405e8cfd953f3d43083a56de2f7a00b3e6e3ec04b0526e3ca7e"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:39:32.751Z","content":{"summary":"Mutates selected object properties by replacing array values with copies that omit undefined entries while leaving other values untouched.","signature_intent":"Normalize referenced property values in a mutable work list by compacting arrays in place on their owning objects.","behavior":["Accepts a mutable list of property references.","Processes references from the end while more than one reference remains.","Replaces each referenced array value with a new array containing only defined entries.","Preserves all non-array referenced values unchanged.","Consumes each processed reference from the input list."],"edge_cases":["An empty or single-reference input list performs no changes.","Sparse array holes are removed because they behave as undefined entries.","Defined falsy values such as null, false, zero, and empty strings are preserved.","Invalid references can throw when the target property cannot be read."],"forbidden_details":["No source identifiers, repository names, paths, or implementation-specific names included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:04:46.023Z","display_code":"function candidate(references) {\n  while (references.length > 1) {\n    const reference = references.pop();\n    let owner;\n    let property;\n\n    if (Array.isArray(reference)) {\n      owner = reference[0];\n      property = reference[1];\n    } else {\n      owner = reference.owner ?? reference.object ?? reference.target;\n      property = reference.key ?? reference.property ?? reference.name;\n    }\n\n    const value = owner[property];\n    if (Array.isArray(value)) {\n      owner[property] = value.filter((entry) => entry !== undefined);\n    }\n  }\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0032","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/utils.js","source_start_line":45,"source_end_line":100,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/utils.js#L45-L100","display_code":"function candidate(target, source, options) {\n    \n    if (!source) {\n        return target;\n    }\n\n    if (typeof source !== 'object') {\n        if (isArray(target)) {\n            target.push(source);\n        } else if (target && typeof target === 'object') {\n            if ((options && (options.plainObjects || options.allowPrototypes)) || !has.call(Object.prototype, source)) {\n                target[source] = true;\n            }\n        } else {\n            return [target, source];\n        }\n\n        return target;\n    }\n\n    if (!target || typeof target !== 'object') {\n        return [target].concat(source);\n    }\n\n    var mergeTarget = target;\n    if (isArray(target) && !isArray(source)) {\n        mergeTarget = arrayToObject(target, options);\n    }\n\n    if (isArray(target) && isArray(source)) {\n        source.forEach(function (item, i) {\n            if (has.call(target, i)) {\n                var targetItem = target[i];\n                if (targetItem && typeof targetItem === 'object' && item && typeof item === 'object') {\n                    target[i] = merge(targetItem, item, options);\n                } else {\n                    target.push(item);\n                }\n            } else {\n                target[i] = item;\n            }\n        });\n        return target;\n    }\n\n    return Object.keys(source).reduce(function (acc, key) {\n        var value = source[key];\n\n        if (has.call(acc, key)) {\n            acc[key] = merge(acc[key], value, options);\n        } else {\n            acc[key] = value;\n        }\n        return acc;\n    }, mergeTarget);\n}","normalized_hash":"sha256-760a85e82b31150ab2f848335aaac1ac678573200bb29eb05b3bd23b94e114a5"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:39:53.645Z","content":{"summary":"Combines two inputs by preserving existing structure where possible and recursively merging compatible collection values.","signature_intent":"Accepts two arbitrary values plus optional configuration and returns a merged value.","behavior":["Returns the first input unchanged when the second input is falsy.","Appends a primitive second input to an array first input.","Adds a primitive second input as a truthy key on an object first input when allowed by safety rules.","Returns a two-item array when neither input can directly receive a primitive value.","When the second input is an object and the first input is not, returns an array containing the first input followed by the second input, expanding arrays.","Merges arrays by index, recursively merging object pairs at matching indexes and appending conflicting primitive or incompatible values.","Merges object keys by recursively combining existing keys and copying new keys."],"edge_cases":["Falsy second inputs are ignored entirely.","Array indexes missing from the first input are assigned directly from the second input.","Merging an array with a non-array object returns an object-like result rather than preserving array behavior.","Primitive keys that match protected prototype properties are skipped unless configuration permits them."],"forbidden_details":["No variable names, repository names, source paths, or low-level implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:06:21.277Z","display_code":"function candidate(first, second, options) {\n  if (!second) {\n    return first;\n  }\n\n  var owns = Object.prototype.hasOwnProperty;\n  var arrays = Array.isArray;\n  var allowProtected = !!(options && (options.plainObjects || options.allowPrototypes || options.allowPrototypeProperties || options.allowUnsafeKeys));\n\n  function objectLike(value) {\n    return value !== null && typeof value === 'object';\n  }\n\n  function keyAllowed(key) {\n    return allowProtected || !owns.call(Object.prototype, key);\n  }\n\n  function makeRecord() {\n    return options && options.plainObjects ? Object.create(null) : {};\n  }\n\n  function join(left, right) {\n    if (!right) {\n      return left;\n    }\n\n    if (!objectLike(right)) {\n      if (arrays(left)) {\n        left.push(right);\n        return left;\n      }\n      if (left && typeof left === 'object') {\n        var label = String(right);\n        if (keyAllowed(label)) {\n          left[label] = true;\n        }\n        return left;\n      }\n      return [left, right];\n    }\n\n    if (!objectLike(left)) {\n      return arrays(right) ? [left].concat(right) : [left, right];\n    }\n\n    if (arrays(left) && arrays(right)) {\n      for (var i = 0; i < right.length; i += 1) {\n        if (!owns.call(right, i)) {\n          continue;\n        }\n        if (owns.call(left, i)) {\n          if (objectLike(left[i]) && objectLike(right[i])) {\n            left[i] = join(left[i], right[i]);\n          } else {\n            left.push(right[i]);\n          }\n        } else {\n          left[i] = right[i];\n        }\n      }\n      return left;\n    }\n\n    var base = left;\n    if (arrays(left) && !arrays(right)) {\n      base = makeRecord();\n      for (var j = 0; j < left.length; j += 1) {\n        if (owns.call(left, j)) {\n          base[j] = left[j];\n        }\n      }\n    }\n\n    var names = Object.keys(right);\n    for (var k = 0; k < names.length; k += 1) {\n      var name = names[k];\n      if (owns.call(base, name)) {\n        base[name] = join(base[name], right[name]);\n      } else {\n        base[name] = right[name];\n      }\n    }\n    return base;\n  }\n\n  return join(first, second);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0033","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/utils.js","source_start_line":123,"source_end_line":184,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/utils.js#L123-L184","display_code":"function candidate(str, defaultEncoder, charset) {\n    \n    \n    if (str.length === 0) {\n        return str;\n    }\n\n    var string = str;\n    if (typeof str === 'symbol') {\n        string = Symbol.prototype.toString.call(str);\n    } else if (typeof str !== 'string') {\n        string = String(str);\n    }\n\n    if (charset === 'iso-8859-1') {\n        return escape(string).replace(/%u[0-9a-f]{4}/gi, function ($0) {\n            return '%26%23' + parseInt($0.slice(2), 16) + '%3B';\n        });\n    }\n\n    var out = '';\n    for (var i = 0; i < string.length; ++i) {\n        var c = string.charCodeAt(i);\n\n        if (\n            c === 0x2D \n            || c === 0x2E \n            || c === 0x5F \n            || c === 0x7E \n            || (c >= 0x30 && c <= 0x39) \n            || (c >= 0x41 && c <= 0x5A) \n            || (c >= 0x61 && c <= 0x7A) \n        ) {\n            out += string.charAt(i);\n            continue;\n        }\n\n        if (c < 0x80) {\n            out = out + hexTable[c];\n            continue;\n        }\n\n        if (c < 0x800) {\n            out = out + (hexTable[0xC0 | (c >> 6)] + hexTable[0x80 | (c & 0x3F)]);\n            continue;\n        }\n\n        if (c < 0xD800 || c >= 0xE000) {\n            out = out + (hexTable[0xE0 | (c >> 12)] + hexTable[0x80 | ((c >> 6) & 0x3F)] + hexTable[0x80 | (c & 0x3F)]);\n            continue;\n        }\n\n        i += 1;\n        c = 0x10000 + (((c & 0x3FF) << 10) | (string.charCodeAt(i) & 0x3FF));\n        out += hexTable[0xF0 | (c >> 18)]\n            + hexTable[0x80 | ((c >> 12) & 0x3F)]\n            + hexTable[0x80 | ((c >> 6) & 0x3F)]\n            + hexTable[0x80 | (c & 0x3F)];\n    }\n\n    return out;\n}","normalized_hash":"sha256-ace9edeb5169f1f5eb5b859dd50fce9fcdcdb665c73a54b02eafc5eb41cd5ce3"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:40:12.326Z","content":{"summary":"Percent-encodes a value into a URL-safe string, with alternate handling when ISO-8859-1 output is requested.","signature_intent":"Accepts a value, an unused encoder argument, and an optional character set indicator; returns either the original empty value or an encoded string.","behavior":["Returns the original value unchanged when its length is zero.","Converts symbols using their symbol string form and other non-string values using standard string conversion.","In ISO-8859-1 mode, applies legacy percent escaping and converts non-Latin-1 Unicode escapes into numeric character references that are themselves percent-encoded.","In the default mode, leaves ASCII letters, digits, hyphen, period, underscore, and tilde unescaped.","In the default mode, percent-encodes other ASCII characters as single bytes.","In the default mode, percent-encodes non-ASCII characters as UTF-8 bytes.","Encodes valid surrogate pairs as four-byte UTF-8 sequences."],"edge_cases":["Null or undefined input throws before conversion because length is read first.","Objects with zero length are returned unchanged instead of being stringified.","Lone surrogate code units are treated as if paired with the following code unit, which can produce nonstandard encoding.","The supplied encoder callback has no observable effect."],"forbidden_details":["Variable names","Repository names","Source paths","Internal table names","Step-by-step implementation quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:06:46.630Z","display_code":"function candidate(value, encoder, charset) {\n  if (value.length === 0) {\n    return value;\n  }\n\n  var text = typeof value === \"symbol\" ? value.toString() : String(value);\n\n  if (charset === \"ISO-8859-1\") {\n    return escape(text).replace(/%u([0-9a-fA-F]{4})/g, function(match, hex) {\n      return \"%26%23\" + parseInt(hex, 16) + \"%3B\";\n    });\n  }\n\n  var result = \"\";\n  for (var i = 0; i < text.length; i++) {\n    var code = text.charCodeAt(i);\n\n    if (\n      (code >= 48 && code <= 57) ||\n      (code >= 65 && code <= 90) ||\n      (code >= 97 && code <= 122) ||\n      code === 45 ||\n      code === 46 ||\n      code === 95 ||\n      code === 126\n    ) {\n      result += text.charAt(i);\n    } else if (code < 128) {\n      result += \"%\" + code.toString(16).toUpperCase().padStart(2, \"0\");\n    } else if (code < 2048) {\n      result += \"%\" + (192 | (code >> 6)).toString(16).toUpperCase();\n      result += \"%\" + (128 | (code & 63)).toString(16).toUpperCase();\n    } else if (code < 55296 || code > 57343) {\n      result += \"%\" + (224 | (code >> 12)).toString(16).toUpperCase();\n      result += \"%\" + (128 | ((code >> 6) & 63)).toString(16).toUpperCase();\n      result += \"%\" + (128 | (code & 63)).toString(16).toUpperCase();\n    } else {\n      i++;\n      var next = text.charCodeAt(i);\n      var point = 65536 + (((code & 1023) << 10) | (next & 1023));\n      result += \"%\" + (240 | (point >> 18)).toString(16).toUpperCase();\n      result += \"%\" + (128 | ((point >> 12) & 63)).toString(16).toUpperCase();\n      result += \"%\" + (128 | ((point >> 6) & 63)).toString(16).toUpperCase();\n      result += \"%\" + (128 | (point & 63)).toString(16).toUpperCase();\n    }\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0034","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"ljharb/qs","source_commit":"8e014a7b1749ccec52104121950e7b0d251caa78","source_path":"lib/utils.js","source_start_line":186,"source_end_line":208,"source_url":"https://github.com/ljharb/qs/blob/8e014a7b1749ccec52104121950e7b0d251caa78/lib/utils.js#L186-L208","display_code":"function candidate(value) {\n    var queue = [{ obj: { o: value }, prop: 'o' }];\n    var refs = [];\n\n    for (var i = 0; i < queue.length; ++i) {\n        var item = queue[i];\n        var obj = item.obj[item.prop];\n\n        var keys = Object.keys(obj);\n        for (var j = 0; j < keys.length; ++j) {\n            var key = keys[j];\n            var val = obj[key];\n            if (typeof val === 'object' && val !== null && refs.indexOf(val) === -1) {\n                queue.push({ obj: obj, prop: key });\n                refs.push(val);\n            }\n        }\n    }\n\n    compactQueue(queue);\n\n    return value;\n}","normalized_hash":"sha256-3723eb002342583a84b56c4bf0aea4865f6afea718068a7d38b5bd375637b878"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:40:29.526Z","content":{"summary":"Traverses the input's reachable own enumerable object-valued properties, runs a compaction step on the collected references, and returns the original input.","signature_intent":"Accepts any JavaScript value and returns the same value reference after traversal and compaction.","behavior":["Enumerates own enumerable string-keyed properties of each visited object.","Follows property values only when they are non-null objects.","Avoids revisiting the same discovered object reference more than once.","Invokes an external compaction step on the collected traversal records before returning.","Returns the original input unchanged by direct assignment."],"edge_cases":["Throws when the initial input is null or undefined.","Primitive inputs are coerced during property enumeration and usually produce no traversal.","Arrays are treated as ordinary objects with enumerable keys.","Functions are not followed unless encountered as enumerable containers through object-valued properties.","Cyclic object graphs terminate because repeated object references are skipped."],"forbidden_details":["No original identifier names, repository names, source paths, or unnecessary implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:07:06.629Z","display_code":"function candidate(input) {\n  var records = [];\n  var seen = typeof Set === \"function\" ? new Set() : null;\n\n  function hasVisited(value) {\n    if (seen) {\n      if (seen.has(value)) return true;\n      seen.add(value);\n      return false;\n    }\n    for (var i = 0; i < records.length; i++) {\n      if (records[i] === value) return true;\n    }\n    return false;\n  }\n\n  function walk(value) {\n    Object.keys(value);\n    if ((typeof value !== \"object\" && typeof value !== \"function\") || value === null) return;\n    if (hasVisited(value)) return;\n    records.push(value);\n\n    var keys = Object.keys(value);\n    for (var i = 0; i < keys.length; i++) {\n      var next = value[keys[i]];\n      if (next !== null && typeof next === \"object\") {\n        walk(next);\n      }\n    }\n  }\n\n  walk(input);\n\n  if (typeof compact === \"function\") {\n    compact(records);\n  } else if (typeof globalThis !== \"undefined\" && typeof globalThis.compact === \"function\") {\n    globalThis.compact(records);\n  }\n\n  return input;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0035","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"bench/bench-dom-bindings.js","source_start_line":5,"source_end_line":15,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/bench/bench-dom-bindings.js#L5-L15","display_code":"function candidate(labelId, updater) {\n  const label = document.getElementById(labelId);\n  const input = label.querySelector(\"input\");\n\n  input.addEventListener(\"input\", e => {\n    e.preventDefault();\n    updater(input.value);\n  });\n\n  updater(input.value);\n}","normalized_hash":"sha256-e1a748e4900490ccdea543d2803be2763aa1a0e4c7e00c35bfc40382db4b6562"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:40:37.617Z","content":{"summary":"Connects a text input inside a selected page element to a callback so the callback receives the current value immediately and after user edits.","signature_intent":"Accepts a DOM element identifier and a value-handling function.","behavior":["Finds a page element by the provided identifier.","Finds the first descendant text input within that element.","Registers an input-event listener on that input.","On each input event, prevents the event default action and passes the input's current value to the provided handler.","Invokes the handler once during setup with the input's current value."],"edge_cases":["Throws if no element exists for the provided identifier.","Throws if the selected element does not contain an input.","The handler may be called with an empty string when the input has no value.","Only the first matching descendant input is observed."],"forbidden_details":["Variable names","Repository names","Source paths","Unnecessary algorithmic quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:07:18.750Z","display_code":"function candidate(identifier, handler) {\n  const container = document.getElementById(identifier);\n  if (!container) {\n    throw new Error(\"Element not found\");\n  }\n\n  const textBox = container.querySelector('input[type=\"text\"], input:not([type])');\n  if (!textBox) {\n    throw new Error(\"Input not found\");\n  }\n\n  const sendValue = () => handler(textBox.value);\n\n  textBox.addEventListener(\"input\", event => {\n    event.preventDefault();\n    sendValue();\n  });\n\n  sendValue();\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0036","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"bench/bench-dom-bindings.js","source_start_line":74,"source_end_line":114,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/bench/bench-dom-bindings.js#L74-L114","display_code":"function candidate(button, results, benchName, bencher) {\n  button.addEventListener(\"click\", async function (e) {\n    e.preventDefault();\n\n    const buttons = [...document.querySelectorAll(\"button\")];\n    buttons.forEach(b => b.setAttribute(\"disabled\", true));\n    results.innerHTML = \"\";\n    await new Promise(r => requestAnimationFrame(r));\n\n    var stats = await bencher();\n\n    buttons.forEach(b => b.removeAttribute(\"disabled\"));\n\n    const csv = stats\n          .xs\n          .map(x => `\"${implAndBrowser}\",${testSourceMap.mappings.length},\"${benchName}\",${x}`)\n          .join(\"\\n\");\n\n    results.innerHTML = `\n      <table>\n        <thead>\n          <tr>\n            <td>Samples</td>\n            <td>Total (${stats.unit})</th>\n            <td>Mean (${stats.unit})</th>\n            <td>Standard Deviation (${stats.unit})</th>\n          </tr>\n        </thead>\n        <tbody>\n          <tr>\n            <td>${stats.samples()}</td>\n            <td>${stats.total().toFixed(2)}</td>\n            <td>${stats.mean().toFixed(2)}</td>\n            <td>${stats.stddev().toFixed(2)}</td>\n          </tr>\n        </tbody>\n      </table>\n      <pre style=\"overflow:scroll;max-height:100px; max-width:500px;outline:1px solid black\">${csv}</pre>\n    `;\n  }, false);\n}","normalized_hash":"sha256-46d52e3acf9922c4ac81a0a3a29c6ef5864aa2c17c9b38b0cbf0da63a474813b"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:40:54.232Z","content":{"summary":"Registers a click-driven benchmark run that prevents the default action, runs an asynchronous measurement, and displays tabular statistics plus per-sample export text.","signature_intent":"Takes a clickable control, an output container, a benchmark label, and an asynchronous measurement callback.","behavior":["Adds a click listener without capturing.","Cancels the clicked control's default action.","Disables all buttons on the page while the measurement is running.","Clears the output container before starting the measurement.","Allows the cleared state to render before invoking the measurement callback.","Re-enables all page buttons after a successful measurement.","Displays sample count, total, mean, and standard deviation using the reported unit.","Rounds total, mean, and standard deviation to two decimal places.","Displays one export row per measured sample, including environment label, mapping count, benchmark label, and sample value."],"edge_cases":["If the measurement callback rejects or throws, buttons remain disabled and no result table is rendered.","If expected statistic methods or fields are missing, rendering fails after the measurement completes.","If external labels or mapping metadata are unavailable, export generation fails.","Existing output content is discarded on every click."],"forbidden_details":["Original identifier names are intentionally omitted.","Repository and source-location details are intentionally omitted.","Low-level construction details are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:07:40.051Z","display_code":"function candidate(control, output, label, measure) {\n  control.addEventListener(\"click\", async function (event) {\n    event.preventDefault();\n\n    var buttons = Array.prototype.slice.call(document.querySelectorAll(\"button\"));\n    buttons.forEach(function (button) {\n      button.disabled = true;\n    });\n\n    output.textContent = \"\";\n\n    await new Promise(function (resolve) {\n      requestAnimationFrame(resolve);\n    });\n\n    var result = await measure();\n\n    buttons.forEach(function (button) {\n      button.disabled = false;\n    });\n\n    var unit = result.unit;\n    var samples = result.samples;\n    var rowData = [\n      [\"samples\", samples.length],\n      [\"total (\" + unit + \")\", result.total().toFixed(2)],\n      [\"mean (\" + unit + \")\", result.mean().toFixed(2)],\n      [\"standard deviation (\" + unit + \")\", result.standardDeviation().toFixed(2)]\n    ];\n\n    var table = document.createElement(\"table\");\n    var body = document.createElement(\"tbody\");\n\n    rowData.forEach(function (entry) {\n      var row = document.createElement(\"tr\");\n      var nameCell = document.createElement(\"th\");\n      var valueCell = document.createElement(\"td\");\n      nameCell.textContent = entry[0];\n      valueCell.textContent = entry[1];\n      row.appendChild(nameCell);\n      row.appendChild(valueCell);\n      body.appendChild(row);\n    });\n\n    table.appendChild(body);\n    output.appendChild(table);\n\n    var environmentLabel = window.environmentLabel;\n    var mappingCount = window.mappings.length;\n    var exportBox = document.createElement(\"textarea\");\n    exportBox.value = samples.map(function (sample) {\n      return [environmentLabel, mappingCount, label, sample].join(\"\\t\");\n    }).join(\"\\n\");\n    output.appendChild(exportBox);\n  });\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0037","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/base64-vlq.js","source_start_line":93,"source_end_line":111,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/base64-vlq.js#L93-L111","display_code":"function candidate(aValue) {\n  let encoded = \"\";\n  let digit;\n\n  let vlq = toVLQSigned(aValue);\n\n  do {\n    digit = vlq & VLQ_BASE_MASK;\n    vlq >>>= VLQ_BASE_SHIFT;\n    if (vlq > 0) {\n      \n      \n      digit |= VLQ_CONTINUATION_BIT;\n    }\n    encoded += base64.encode(digit);\n  } while (vlq > 0);\n\n  return encoded;\n}","normalized_hash":"sha256-8fee86d0313ffdea4898fe558937a580a7d7fd6c20a1c4284bac3f7e76ef1c16"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:41:16.422Z","content":{"summary":"Encodes a signed integer into a Base64 VLQ string.","signature_intent":"Accepts one signed integer-like value and returns a string representation in Base64 VLQ format.","behavior":["Converts the signed input into the VLQ signed-number representation.","Emits one or more Base64 characters representing the encoded value.","Uses continuation markers for all output chunks except the final chunk.","Returns the complete encoded string."],"edge_cases":["Zero produces a single-character encoding.","Small magnitudes produce a single-character encoding.","Larger magnitudes produce multi-character encodings.","Negative values are represented distinctly from positive values of the same magnitude."],"forbidden_details":["Do not mention local identifier names.","Do not mention repository or file location.","Do not describe incidental loop structure or temporary storage."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:07:54.159Z","display_code":"function candidate(input) {\n  const alphabet = \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\";\n  const number = Math.trunc(Number(input));\n  let remaining = number < 0 ? Math.abs(number) * 2 + 1 : number * 2;\n  let encoded = \"\";\n\n  do {\n    let chunk = remaining % 32;\n    remaining = Math.floor(remaining / 32);\n    if (remaining > 0) chunk += 32;\n    encoded += alphabet[chunk];\n  } while (remaining > 0);\n\n  return encoded;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0038","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/binary-search.js","source_start_line":24,"source_end_line":65,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/binary-search.js#L24-L65","display_code":"function candidate(aLow, aHigh, aNeedle, aHaystack, aCompare, aBias) {\n  \n  \n  \n  \n  \n  \n  \n  \n  \n  const mid = Math.floor((aHigh - aLow) / 2) + aLow;\n  const cmp = aCompare(aNeedle, aHaystack[mid], true);\n  if (cmp === 0) {\n    \n    return mid;\n  } else if (cmp > 0) {\n    \n    if (aHigh - mid > 1) {\n      \n      return recursiveSearch(mid, aHigh, aNeedle, aHaystack, aCompare, aBias);\n    }\n\n    \n    \n    if (aBias == exports.LEAST_UPPER_BOUND) {\n      return aHigh < aHaystack.length ? aHigh : -1;\n    }\n    return mid;\n  }\n\n  \n  if (mid - aLow > 1) {\n    \n    return recursiveSearch(aLow, mid, aNeedle, aHaystack, aCompare, aBias);\n  }\n\n  \n  if (aBias == exports.LEAST_UPPER_BOUND) {\n    return mid;\n  }\n  return aLow < 0 ? -1 : aLow;\n}","normalized_hash":"sha256-f28ba57c7a0b6602b4871ad7b74e82fe964658bccdafa5696940c22b98f8e7a5"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:41:34.380Z","content":{"summary":"Searches a sorted range and returns the index of an exact match or the nearest valid fallback according to the configured bound policy.","signature_intent":"Accepts a bounded search interval, target value, sorted collection, comparator, and bound policy; returns an index or -1.","behavior":["Returns the inspected index immediately when the comparator reports equality.","Continues searching the upper portion when the target sorts after the inspected item and more than one position remains.","Continues searching the lower portion when the target sorts before the inspected item and more than one position remains.","In upper-bound mode, returns the nearest not-less-than index when it is within the collection.","In non-upper-bound mode, returns the nearest not-greater-than index when it is valid.","Returns -1 when the selected fallback position is outside the valid collection range."],"edge_cases":["An upper-bound fallback beyond the collection length returns -1.","A lower fallback before the start of the collection returns -1.","Adjacent search bounds stop recursion and force fallback selection."],"forbidden_details":["No original variable names, repository names, or source paths are included.","No incidental implementation details beyond externally visible behavior are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:08:47.405Z","display_code":"function candidate(start, stop, value, list, compare, mode) {\n  function isUpperChoice(flag) {\n    if (flag === true || flag === 2) {\n      return true;\n    }\n    if (typeof exports === \"object\" && exports && flag == exports.LEAST_UPPER_BOUND) {\n      return true;\n    }\n    if (typeof flag === \"string\") {\n      var normalized = flag.toLowerCase();\n      return normalized === \"upper\" || normalized === \"least_upper_bound\" || normalized === \"least-upper-bound\";\n    }\n    return false;\n  }\n\n  var left = start;\n  var right = stop;\n  var preferUpper = isUpperChoice(mode);\n\n  while (true) {\n    var index = left + Math.floor((right - left) / 2);\n    var ordering = compare(value, list[index], true);\n\n    if (ordering === 0) {\n      return index;\n    }\n\n    if (ordering > 0) {\n      if (right - index > 1) {\n        left = index;\n        continue;\n      }\n      return preferUpper ? (right < list.length ? right : -1) : index;\n    }\n\n    if (index - left > 1) {\n      right = index;\n      continue;\n    }\n\n    return preferUpper ? index : (left < 0 ? -1 : left);\n  }\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0039","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/binary-search.js","source_start_line":85,"source_end_line":107,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/binary-search.js#L85-L107","display_code":"function candidate(aNeedle, aHaystack, aCompare, aBias) {\n  if (aHaystack.length === 0) {\n    return -1;\n  }\n\n  let index = recursiveSearch(-1, aHaystack.length, aNeedle, aHaystack,\n                              aCompare, aBias || exports.GREATEST_LOWER_BOUND);\n  if (index < 0) {\n    return -1;\n  }\n\n  \n  \n  \n  while (index - 1 >= 0) {\n    if (aCompare(aHaystack[index], aHaystack[index - 1], true) !== 0) {\n      break;\n    }\n    --index;\n  }\n\n  return index;\n}","normalized_hash":"sha256-686399d16dc75c6aa8d00a4c87d5bff029fb1f2eb1c60ed617507c5659d6bb2f"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:41:46.644Z","content":{"summary":"Searches an ordered collection for a target and returns the earliest zero-based position among adjacent equivalent matches, or -1 when no suitable match exists.","signature_intent":"Accepts a target value, an ordered collection, a comparison callback, and an optional search-bias mode; returns a numeric index.","behavior":["Returns -1 when the collection is empty.","Uses the provided comparison callback to locate a suitable position according to the requested bias.","Uses a greatest-lower-bound style bias when no bias is supplied.","Returns -1 when the search finds no suitable position.","If a suitable position is found, moves to the first adjacent item considered equivalent by the comparison callback.","Returns the earliest equivalent index as a zero-based number."],"edge_cases":["Empty collection returns -1.","Missing bias uses the default lower-bound behavior.","No suitable search result returns -1.","Multiple adjacent equivalent items return the first such position."],"forbidden_details":["Do not describe internal recursion.","Do not mention implementation variable names.","Do not mention source locations or repository identity.","Do not include incidental formatting or comment details."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:09:40.344Z","display_code":"function candidate(target, collection, compare, bias) {\n  if (collection.length === 0) {\n    return -1;\n  }\n\n  const wantsUpper = bias == 2 || bias === \"LEAST_UPPER_BOUND\" || bias === \"least-upper-bound\";\n  let left = -1;\n  let right = collection.length;\n  let found = -1;\n\n  while (right - left > 1) {\n    const middle = left + Math.floor((right - left) / 2);\n    const order = compare(target, collection[middle], true);\n\n    if (order === 0) {\n      found = middle;\n      break;\n    }\n\n    if (order > 0) {\n      left = middle;\n    } else {\n      right = middle;\n    }\n  }\n\n  let result = found;\n  if (result < 0) {\n    result = wantsUpper ? right : left;\n  }\n\n  if (result < 0 || result >= collection.length) {\n    return -1;\n  }\n\n  while (result > 0 && compare(collection[result], collection[result - 1], true) === 0) {\n    result -= 1;\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0040","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/mapping-list.js","source_start_line":14,"source_end_line":22,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/mapping-list.js#L14-L22","display_code":"function candidate(mappingA, mappingB) {\n  \n  const lineA = mappingA.generatedLine;\n  const lineB = mappingB.generatedLine;\n  const columnA = mappingA.generatedColumn;\n  const columnB = mappingB.generatedColumn;\n  return lineB > lineA || lineB == lineA && columnB >= columnA ||\n         util.compareByGeneratedPositionsInflated(mappingA, mappingB) <= 0;\n}","normalized_hash":"sha256-9b93c1955cee1cc5dc7e03dc4e52c15eeeb3dc040893f2fe57f661db916ade2a"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:41:59.169Z","content":{"summary":"Determines whether the second source-mapping position should be treated as at or after the first in generated-output order.","signature_intent":"Accepts two position-like mapping records and returns a boolean ordering result.","behavior":["Returns true when the second record is on a later generated line.","Returns true when both records are on the same generated line and the second column is the same or later.","Returns true when the generated-position comparator considers the first record not greater than the second.","Returns false only when all ordering checks indicate the second record comes before the first."],"edge_cases":["Equal line and column values return true.","A second record on an earlier line can still return true if the comparator allows it.","A second record on the same line with an earlier column can still return true if the comparator allows it."],"forbidden_details":[]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:09:57.471Z","display_code":"function candidate(first, second) {\n  if (second.generatedLine > first.generatedLine) {\n    return true;\n  }\n\n  if (second.generatedLine === first.generatedLine && second.generatedColumn >= first.generatedColumn) {\n    return true;\n  }\n\n  const compare = typeof candidate.compareGenerated === \"function\"\n    ? candidate.compareGenerated\n    : defaultGeneratedCompare;\n\n  return compare(first, second) <= 0;\n}\n\nfunction defaultGeneratedCompare(left, right) {\n  const leftLine = Number(left.generatedLine);\n  const rightLine = Number(right.generatedLine);\n\n  if (leftLine !== rightLine) {\n    return leftLine - rightLine;\n  }\n\n  const leftColumn = Number(left.generatedColumn);\n  const rightColumn = Number(right.generatedColumn);\n\n  if (leftColumn !== rightColumn) {\n    return leftColumn - rightColumn;\n  }\n\n  return 0;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0041","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/read-wasm.js","source_start_line":23,"source_end_line":35,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/read-wasm.js#L23-L35","display_code":"function candidate() {\n    return new Promise((resolve, reject) => {\n      const wasmPath = path.join(__dirname, \"mappings.wasm\");\n      fs.readFile(wasmPath, null, (error, data) => {\n        if (error) {\n          reject(error);\n          return;\n        }\n\n        resolve(data.buffer);\n      });\n    });\n  }","normalized_hash":"sha256-782cc50fc1b895ffa80e40a88a3856c14c01c29e387bdf882afa8f8960af5101"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:42:08.281Z","content":{"summary":"Returns a promise that resolves with the underlying binary buffer of a bundled WebAssembly file or rejects if the file cannot be read.","signature_intent":"A zero-argument asynchronous helper for loading a local WebAssembly binary into memory.","behavior":["Creates and returns a promise.","Reads a fixed WebAssembly file located alongside the executing module.","Resolves with the file data's underlying ArrayBuffer when reading succeeds.","Rejects with the read error when reading fails."],"edge_cases":["Rejects if the file is missing or inaccessible.","Rejects if filesystem reading reports any error.","The resolved buffer may reference the full backing buffer of the file data."],"forbidden_details":["Variable names","Repository names","Source paths","Unnecessary implementation quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:10:11.443Z","display_code":"function candidate() {\n  const fs = require(\"fs\");\n  const path = require(\"path\");\n\n  return new Promise((resolve, reject) => {\n    fs.readFile(path.join(__dirname, \"bundle.wasm\"), (error, data) => {\n      if (error) {\n        reject(error);\n        return;\n      }\n\n      resolve(data.buffer);\n    });\n  });\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0043","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/source-node.js","source_start_line":151,"source_end_line":164,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/source-node.js#L151-L164","display_code":"function candidate(mapping, code) {\n      if (mapping === null || mapping.source === undefined) {\n        node.add(code);\n      } else {\n        const source = aRelativePath\n          ? util.join(aRelativePath, mapping.source)\n          : mapping.source;\n        node.add(new SourceNode(mapping.originalLine,\n                                mapping.originalColumn,\n                                source,\n                                code,\n                                mapping.name));\n      }\n    }","normalized_hash":"sha256-e65e73f1c149a5f7c05a62b145eabb5cad805599b7fde2c74cf7599781e4340d"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:42:29.950Z","content":{"summary":"Adds generated content to an output structure, either plain or annotated with original-source metadata when available.","signature_intent":"Accepts an optional source-mapping record and a generated content segment, then records the segment in the current output node.","behavior":["Appends the content without metadata when no usable mapping is provided.","Appends the content with original line, column, source reference, and name metadata when a usable mapping is provided.","Uses a configured base reference to qualify the attached source reference when one is available."],"edge_cases":["Null mapping information is treated as absent.","Mapping information without a source reference is treated as absent.","Missing optional name metadata is passed through as absent."],"forbidden_details":["Original identifier names are intentionally omitted.","Repository and source file locations are intentionally omitted.","Low-level construction details are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:10:43.314Z","display_code":"function candidate(mapping, segment) {\n  var target = this && (this.node || this.output || this.current || this);\n  var base = this && (this.baseReference || this.base || this.relativePath || this.sourceRoot);\n\n  if (!mapping || mapping.source == null) {\n    return target.add(segment);\n  }\n\n  var source = mapping.source;\n  if (base) {\n    if (typeof require === \"function\") {\n      try {\n        var path = require(\"path\");\n        source = path.posix ? path.posix.join(base, source) : path.join(base, source);\n      } catch (e) {\n        source = String(base).replace(/\\/$/, \"\") + \"/\" + String(source).replace(/^\\//, \"\");\n      }\n    } else {\n      source = String(base).replace(/\\/$/, \"\") + \"/\" + String(source).replace(/^\\//, \"\");\n    }\n  }\n\n  if (typeof SourceNode !== \"undefined\") {\n    return target.add(new SourceNode(\n      mapping.originalLine,\n      mapping.originalColumn,\n      source,\n      segment,\n      mapping.name\n    ));\n  }\n\n  return target.add({\n    line: mapping.originalLine,\n    column: mapping.originalColumn,\n    source: source,\n    name: mapping.name,\n    content: segment\n  });\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0044","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":32,"source_end_line":44,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L32-L44","display_code":"function candidate(aUrl) {\n  const match = aUrl.match(urlRegexp);\n  if (!match) {\n    return null;\n  }\n  return {\n    scheme: match[1],\n    auth: match[2],\n    host: match[3],\n    port: match[4],\n    path: match[5]\n  };\n}","normalized_hash":"sha256-6f924ddf5dee3c29354576b53b266647cba97ae145430a5f3c5ab91b543db486"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:42:39.989Z","content":{"summary":"Parses a URL-like string into its recognized components, or returns null when it does not match the expected URL format.","signature_intent":"Accepts a URL string and returns either a structured component object or null.","behavior":["Returns null for input that is not recognized as a valid URL-like value.","Returns an object containing the parsed protocol, optional credentials, host, optional port, and path components.","Preserves parsed component text as captured from the input."],"edge_cases":["Unmatched input produces null instead of throwing.","Optional URL parts may be absent in the returned structure depending on the accepted format."],"forbidden_details":["Function identifier","Local identifiers","Repository name","Source path","Specific parsing implementation details"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:11:03.538Z","display_code":"function candidate(value) {\n  if (typeof value !== \"string\") {\n    return null;\n  }\n\n  const parsed = value.match(/^([A-Za-z][A-Za-z0-9+.-]*):\\/\\/(?:(?:([^@/?#]*)@))?(\\[[^\\]]+\\]|[^:/?#]+)(?::([0-9]+))?([^?#]*)?(?:[?#].*)?$/);\n\n  if (!parsed) {\n    return null;\n  }\n\n  const result = {\n    protocol: parsed[1],\n    host: parsed[3]\n  };\n\n  if (parsed[2] !== undefined) {\n    result.credentials = parsed[2];\n  }\n\n  if (parsed[4] !== undefined) {\n    result.port = parsed[4];\n  }\n\n  if (parsed[5] !== undefined) {\n    result.path = parsed[5];\n  }\n\n  return result;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0045","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":47,"source_end_line":66,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L47-L66","display_code":"function candidate(aParsedUrl) {\n  let url = \"\";\n  if (aParsedUrl.scheme) {\n    url += aParsedUrl.scheme + \":\";\n  }\n  url += \"\n  if (aParsedUrl.auth) {\n    url += aParsedUrl.auth + \"@\";\n  }\n  if (aParsedUrl.host) {\n    url += aParsedUrl.host;\n  }\n  if (aParsedUrl.port) {\n    url += \":\" + aParsedUrl.port;\n  }\n  if (aParsedUrl.path) {\n    url += aParsedUrl.path;\n  }\n  return url;\n}","normalized_hash":"sha256-d1c4b76fcf383627587fd6d1ec01a296e24b5d2ed6c3e57a61c380509cdf2ecd"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:49:02.456Z","content":{"summary":"Serializes a parsed URL-like value into a URL string from its available components.","signature_intent":"Accepts a structured URL representation and returns its string form.","behavior":["Adds a scheme followed by a colon when a scheme is present.","Includes the authority delimiter before authority-related components.","Adds credentials followed by an at sign when credentials are present.","Appends the network location when present.","Adds a colon-prefixed port when a port is present.","Appends the path when present.","Returns the assembled string."],"edge_cases":["Missing optional components are skipped.","A port without a network location is still colon-prefixed.","Credentials without a network location are still followed by an at sign.","With no components beyond the delimiter, the result is only the authority delimiter."],"forbidden_details":[]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:11:24.666Z","display_code":"function candidate(input) {\n  const value = input || {};\n  let output = \"\";\n\n  const read = (...keys) => {\n    for (const key of keys) {\n      if (value[key] !== undefined && value[key] !== null && value[key] !== \"\") {\n        return String(value[key]);\n      }\n    }\n    return \"\";\n  };\n\n  const scheme = read(\"scheme\", \"protocol\");\n  const credentials = read(\"credentials\", \"auth\", \"userinfo\", \"userInfo\");\n  const location = read(\"netloc\", \"networkLocation\", \"host\", \"hostname\");\n  const port = read(\"port\");\n  const path = read(\"path\", \"pathname\");\n\n  if (scheme) output += scheme.endsWith(\":\") ? scheme : scheme + \":\";\n  if (value.slashes || value.authorityDelimiter || value.hasAuthority || credentials || location || port) output += \"//\";\n  if (credentials) output += credentials + \"@\";\n  if (location) output += location;\n  if (port) output += \":\" + String(port).replace(/^:/, \"\");\n  if (path) output += path;\n\n  return output;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0046","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":78,"source_end_line":104,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L78-L104","display_code":"function candidate(f) {\n  const cache = [];\n\n  return function(input) {\n    for (let i = 0; i < cache.length; i++) {\n      if (cache[i].input === input) {\n        const temp = cache[0];\n        cache[0] = cache[i];\n        cache[i] = temp;\n        return cache[0].result;\n      }\n    }\n\n    const result = f(input);\n\n    cache.unshift({\n      input,\n      result,\n    });\n\n    if (cache.length > MAX_CACHED_INPUTS) {\n      cache.pop();\n    }\n\n    return result;\n  };\n}","normalized_hash":"sha256-024d560420734214911db07cbf984115e3e309fc677a920ce3f03bbef6741f7d"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:49:18.647Z","content":{"summary":"Wraps a single-argument callable with bounded memoization so repeated arguments can reuse previously computed return values.","signature_intent":"Accepts a unary callable and returns a unary callable with the same argument and return behavior plus memoized reuse.","behavior":["Calls the wrapped callable when an argument has not been seen within the retained history.","Returns the previously computed value when the same argument is seen again.","Recognizes matches using strict equality semantics.","Prioritizes recently reused entries for retention.","Discards the least recently retained entry when the configured capacity is exceeded."],"edge_cases":["Object arguments match only by reference, not by structural equality.","Thrown errors from the wrapped callable are propagated and not retained.","Falsy arguments and falsy return values are handled normally.","If capacity is zero, each call computes fresh but still returns the computed value."],"forbidden_details":["Internal identifiers are intentionally omitted.","Source and repository details are intentionally omitted.","Implementation-only ordering mechanics are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:11:41.458Z","display_code":"function candidate(fn, capacity) {\n  const max = capacity === undefined ? 1 : Math.max(0, Math.floor(capacity));\n  const entries = [];\n\n  return function(arg) {\n    for (let i = 0; i < entries.length; i += 1) {\n      if (entries[i].key === arg) {\n        const hit = entries[i];\n        if (i !== entries.length - 1) {\n          entries.splice(i, 1);\n          entries.push(hit);\n        }\n        return hit.value;\n      }\n    }\n\n    const value = fn(arg);\n\n    if (max > 0) {\n      entries.push({ key: arg, value: value });\n      if (entries.length > max) {\n        entries.shift();\n      }\n    }\n\n    return value;\n  };\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0047","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":117,"source_end_line":178,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L117-L178","display_code":"function candidate(aPath) {\n  let path = aPath;\n  const url = urlParse(aPath);\n  if (url) {\n    if (!url.path) {\n      return aPath;\n    }\n    path = url.path;\n  }\n  const isAbsolute = exports.isAbsolute(path);\n\n  \n  \n  const parts = [];\n  let start = 0;\n  let i = 0;\n  while (true) {\n    start = i;\n    i = path.indexOf(\"/\", start);\n    if (i === -1) {\n      parts.push(path.slice(start));\n      break;\n    } else {\n      parts.push(path.slice(start, i));\n      while (i < path.length && path[i] === \"/\") {\n        i++;\n      }\n    }\n  }\n\n  let up = 0;\n  for (i = parts.length - 1; i >= 0; i--) {\n    const part = parts[i];\n    if (part === \".\") {\n      parts.splice(i, 1);\n    } else if (part === \"..\") {\n      up++;\n    } else if (up > 0) {\n      if (part === \"\") {\n        \n        \n        \n        parts.splice(i + 1, up);\n        up = 0;\n      } else {\n        parts.splice(i, 2);\n        up--;\n      }\n    }\n  }\n  path = parts.join(\"/\");\n\n  if (path === \"\") {\n    path = isAbsolute ? \"/\" : \".\";\n  }\n\n  if (url) {\n    url.path = path;\n    return urlGenerate(url);\n  }\n  return path;\n}","normalized_hash":"sha256-1b4e4dc276e502ec8008119dd09bd8b4128100cbcaff7d76e5c6d96d3d6e8a86"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:49:26.504Z","content":{"summary":"Normalizes a path or URL-like string by resolving dot segments and redundant separators while preserving absolute and URL structure.","signature_intent":"Accepts a single string-like path or URL input and returns a normalized string.","behavior":["Parses URL-like inputs and normalizes only their path component when present.","Returns URL-like inputs unchanged when they contain no path component.","Collapses repeated forward slashes within the normalized path.","Removes current-directory segments.","Resolves parent-directory segments against preceding normal segments.","Preserves leading absolute-path semantics.","Returns a regenerated URL-like string when the input was parsed as URL-like.","Returns a normalized path string for non-URL inputs."],"edge_cases":["An empty normalized absolute path becomes \"/\".","An empty normalized relative path becomes \".\".","Leading parent-directory traversal in relative paths is preserved when it cannot be resolved.","Parent-directory traversal at an absolute root does not remove the root.","Trailing separators may be removed during normalization."],"forbidden_details":["Variable names","Repository names","Source paths","Incidental implementation mechanics"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:12:01.270Z","display_code":"function candidate(value) {\n  const source = String(value);\n\n  function cleanPath(path) {\n    const absolute = path.startsWith(\"/\");\n    const parts = path.split(\"/\");\n    const kept = [];\n\n    for (const part of parts) {\n      if (part === \"\" || part === \".\") {\n        continue;\n      }\n      if (part === \"..\") {\n        if (kept.length && kept[kept.length - 1] !== \"..\") {\n          kept.pop();\n        } else if (!absolute) {\n          kept.push(\"..\");\n        }\n      } else {\n        kept.push(part);\n      }\n    }\n\n    if (absolute) {\n      return kept.length ? \"/\" + kept.join(\"/\") : \"/\";\n    }\n    return kept.length ? kept.join(\"/\") : \".\";\n  }\n\n  const urlMatch = source.match(/^([A-Za-z][A-Za-z0-9+.-]*:)(\\/\\/[^/?#]*)?([^?#]*)(\\?[^#]*)?(#.*)?$/);\n\n  if (urlMatch && (urlMatch[2] || source.indexOf(\":\") !== -1)) {\n    const scheme = urlMatch[1];\n    const authority = urlMatch[2] || \"\";\n    const path = urlMatch[3] || \"\";\n    const query = urlMatch[4] || \"\";\n    const fragment = urlMatch[5] || \"\";\n\n    if (path === \"\") {\n      return source;\n    }\n\n    return scheme + authority + cleanPath(path) + query + fragment;\n  }\n\n  return cleanPath(source);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0048","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":197,"source_end_line":237,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L197-L237","display_code":"function candidate(aRoot, aPath) {\n  if (aRoot === \"\") {\n    aRoot = \".\";\n  }\n  if (aPath === \"\") {\n    aPath = \".\";\n  }\n  const aPathUrl = urlParse(aPath);\n  const aRootUrl = urlParse(aRoot);\n  if (aRootUrl) {\n    aRoot = aRootUrl.path || \"/\";\n  }\n\n  \n  if (aPathUrl && !aPathUrl.scheme) {\n    if (aRootUrl) {\n      aPathUrl.scheme = aRootUrl.scheme;\n    }\n    return urlGenerate(aPathUrl);\n  }\n\n  if (aPathUrl || aPath.match(dataUrlRegexp)) {\n    return aPath;\n  }\n\n  \n  if (aRootUrl && !aRootUrl.host && !aRootUrl.path) {\n    aRootUrl.host = aPath;\n    return urlGenerate(aRootUrl);\n  }\n\n  const joined = aPath.charAt(0) === \"/\"\n    ? aPath\n    : normalize(aRoot.replace(/\\/+$/, \"\") + \"/\" + aPath);\n\n  if (aRootUrl) {\n    aRootUrl.path = joined;\n    return urlGenerate(aRootUrl);\n  }\n  return joined;\n}","normalized_hash":"sha256-b58ac07594280dfad2477c6199c3de89b56dc4e91516c711cbdbc6fb56b9d59d"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:49:42.561Z","content":{"summary":"Combines a base location and a target location into a normalized path or URL while preserving complete URL and data URL inputs.","signature_intent":"Accepts two string locations and returns a resolved string location.","behavior":["Treats an empty first or second input as the current directory marker.","Returns complete URL or data URL second inputs unchanged.","For scheme-relative second inputs, returns a generated URL and inherits the first input's scheme when available.","When the first input is a URL, preserves its URL components and replaces its path with the resolved path.","For relative second inputs, joins them to the first input and normalizes the resulting path.","For absolute-path second inputs, uses that path directly, preserving URL components from the first input when present."],"edge_cases":["An empty first input behaves like a relative current directory.","An empty second input resolves as the current directory.","A URL first input without host or path can produce a URL using the second input as the host.","A URL first input with no explicit path uses slash as its path base."],"forbidden_details":[]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:12:27.095Z","display_code":"function candidate(first, second) {\n  first = first || \".\";\n  second = second || \".\";\n\n  if (/^(?:data:|[A-Za-z][A-Za-z0-9+.-]*:)/.test(second)) {\n    return second;\n  }\n\n  function parseLocation(value) {\n    var match = /^([A-Za-z][A-Za-z0-9+.-]*:)?(?:\\/\\/([^/?#]*))?([^?#]*)(\\?[^#]*)?(#.*)?$/.exec(value);\n    return {\n      scheme: match && match[1] ? match[1] : \"\",\n      host: match && match[2] != null ? match[2] : null,\n      path: match && match[3] ? match[3] : \"\",\n      query: match && match[4] ? match[4] : \"\",\n      hash: match && match[5] ? match[5] : \"\"\n    };\n  }\n\n  function normalize(path) {\n    var absolute = path.charAt(0) === \"/\";\n    var trailing = path.length > 1 && path.charAt(path.length - 1) === \"/\";\n    var parts = path.split(\"/\");\n    var out = [];\n\n    for (var i = 0; i < parts.length; i++) {\n      var part = parts[i];\n      if (!part || part === \".\") continue;\n      if (part === \"..\") {\n        if (out.length && out[out.length - 1] !== \"..\") {\n          out.pop();\n        } else if (!absolute) {\n          out.push(\"..\");\n        }\n      } else {\n        out.push(part);\n      }\n    }\n\n    var result = (absolute ? \"/\" : \"\") + out.join(\"/\");\n    if (!result) result = absolute ? \"/\" : \".\";\n    if (trailing && result !== \"/\") result += \"/\";\n    return result;\n  }\n\n  function directoryOf(path) {\n    if (!path) return \"/\";\n    if (path.charAt(path.length - 1) === \"/\") return path;\n    var index = path.lastIndexOf(\"/\");\n    return index < 0 ? \"\" : path.slice(0, index + 1);\n  }\n\n  var base = parseLocation(first);\n\n  if (second.slice(0, 2) === \"//\") {\n    return (base.scheme || \"\") + second;\n  }\n\n  var nextPath;\n  if (second.charAt(0) === \"/\") {\n    nextPath = normalize(second);\n  } else if (base.scheme && base.host === \"\" && !base.path && second.indexOf(\"/\") < 0 && second.charAt(0) !== \".\") {\n    base.host = second;\n    nextPath = \"/\";\n  } else {\n    nextPath = normalize(directoryOf(base.path || (base.scheme ? \"/\" : \".\")) + second);\n  }\n\n  if (base.scheme || base.host !== null) {\n    return base.scheme + (base.host !== null ? \"//\" + base.host : \"\") + nextPath + base.query + base.hash;\n  }\n\n  return nextPath;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0049","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":250,"source_end_line":281,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L250-L281","display_code":"function candidate(aRoot, aPath) {\n  if (aRoot === \"\") {\n    aRoot = \".\";\n  }\n\n  aRoot = aRoot.replace(/\\/$/, \"\");\n\n  \n  \n  \n  \n  let level = 0;\n  while (aPath.indexOf(aRoot + \"/\") !== 0) {\n    const index = aRoot.lastIndexOf(\"/\");\n    if (index < 0) {\n      return aPath;\n    }\n\n    \n    \n    \n    aRoot = aRoot.slice(0, index);\n    if (aRoot.match(/^([^\\/]+:\\/)?\\/*$/)) {\n      return aPath;\n    }\n\n    ++level;\n  }\n\n  \n  return Array(level + 1).join(\"../\") + aPath.substr(aRoot.length + 1);\n}","normalized_hash":"sha256-d4b415a9712c4df9e4a527b8dece91a11d370ed90d30c71f9b2f6730d26c7b07"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:49:58.758Z","content":{"summary":"Produces a relative path from a directory-like starting point to a contained target, or returns the target unchanged when no suitable relationship can be established.","signature_intent":"Accepts two string path inputs and returns a string path result.","behavior":["Treats an empty starting point as the current directory marker.","Normalizes one trailing slash from the starting point before comparison.","When the target is inside the starting point, returns the target portion relative to it.","When the target is inside an ancestor of the starting point, prefixes the result with one parent-directory segment per ancestor step.","Returns the target unchanged when no usable shared ancestor is found."],"edge_cases":["Empty starting point only works as relative current-directory input when the target begins accordingly.","A starting point without a parent segment causes the target to be returned unchanged if it is not already a prefix match.","Filesystem or URL-like root boundaries are treated as a stop condition and return the target unchanged."],"forbidden_details":["Variable names","Repository names","Source paths","Step-by-step implementation quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:12:43.009Z","display_code":"function candidate(start, target) {\n  let base = start === \"\" ? \".\" : start;\n\n  if (base.length > 0 && base.endsWith(\"/\")) {\n    base = base.slice(0, -1);\n  }\n\n  let prefix = \"\";\n\n  while (true) {\n    if (target === base) {\n      return prefix || \".\";\n    }\n\n    if (target.startsWith(base + \"/\")) {\n      return prefix + target.slice(base.length + 1);\n    }\n\n    const slash = base.lastIndexOf(\"/\");\n\n    if (slash <= 0 || base.slice(0, slash).endsWith(\":/\")) {\n      return target;\n    }\n\n    base = base.slice(0, slash);\n    prefix += \"../\";\n  }\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0050","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":320,"source_end_line":352,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L320-L352","display_code":"function candidate(s) {\n  if (!s) {\n    return false;\n  }\n\n  const length = s.length;\n\n  if (length < 9 ) {\n    return false;\n  }\n\n  \n  if (s.charCodeAt(length - 1) !== 95   ||\n      s.charCodeAt(length - 2) !== 95   ||\n      s.charCodeAt(length - 3) !== 111  ||\n      s.charCodeAt(length - 4) !== 116  ||\n      s.charCodeAt(length - 5) !== 111  ||\n      s.charCodeAt(length - 6) !== 114  ||\n      s.charCodeAt(length - 7) !== 112  ||\n      s.charCodeAt(length - 8) !== 95   ||\n      s.charCodeAt(length - 9) !== 95  ) {\n    return false;\n  }\n  \n\n  for (let i = length - 10; i >= 0; i--) {\n    if (s.charCodeAt(i) !== 36 ) {\n      return false;\n    }\n  }\n\n  return true;\n}","normalized_hash":"sha256-95461de884e6595c498f6e8e3f2e0b51d97a2561a56db87d84fd0504cd68cffe"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:50:22.023Z","content":{"summary":"Returns whether a provided value is a string-like marker made of zero or more dollar signs followed by the exact suffix `__proto__`.","signature_intent":"Validate a special marker format.","behavior":["Returns `false` for falsy input.","Returns `false` when the length is less than 9.","Returns `true` for `__proto__`.","Returns `true` when every leading character before `__proto__` is `$`.","Returns `false` for any other suffix or any non-`$` leading character."],"edge_cases":["An exact length of 9 is valid only for `__proto__`.","Additional trailing characters after `__proto__` make the result `false`.","Truthy non-string values without compatible character access can throw instead of returning `false`."],"forbidden_details":["Variable names, repository names, source paths, and low-level character-code checks are intentionally omitted."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:12:58.870Z","display_code":"function candidate(value) {\n  if (!value) return false;\n\n  const marker = \"__proto__\";\n  const count = value.length;\n\n  if (count < marker.length) return false;\n\n  const start = count - marker.length;\n\n  for (let index = 0; index < start; index++) {\n    if (value[index] !== \"$\") return false;\n  }\n\n  for (let index = 0; index < marker.length; index++) {\n    if (value[start + index] !== marker[index]) return false;\n  }\n\n  return true;\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0051","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":362,"source_end_line":389,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L362-L389","display_code":"function candidate(mappingA, mappingB, onlyCompareOriginal) {\n  let cmp = strcmp(mappingA.source, mappingB.source);\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalLine - mappingB.originalLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalColumn - mappingB.originalColumn;\n  if (cmp !== 0 || onlyCompareOriginal) {\n    return cmp;\n  }\n\n  cmp = mappingA.generatedColumn - mappingB.generatedColumn;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.generatedLine - mappingB.generatedLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  return strcmp(mappingA.name, mappingB.name);\n}","normalized_hash":"sha256-32dd1d2a517f888c90b5a33c9f8858a4564e7b818552ff5068b4f645e57beadc"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:50:33.161Z","content":{"summary":"Compares two mapping records to produce a deterministic sort order based on original position first and generated position/name only when requested.","signature_intent":"Accepts two mapping-like records and a boolean that limits comparison to original-location fields, returning a negative, zero, or positive ordering value.","behavior":["Orders first by source identifier using string comparison.","Then orders by original line number.","Then orders by original column number.","When original-only comparison is enabled, stops after original column comparison.","Otherwise orders by generated column number, then generated line number.","Finally orders by optional name using string comparison.","Returns zero when all compared fields are equivalent."],"edge_cases":["Missing or non-string text fields follow the underlying string comparison behavior.","Missing or non-numeric position fields can produce non-standard numeric comparison results.","When original-only comparison is enabled, records with matching original fields compare as equal even if generated fields or names differ."],"forbidden_details":["No variable names, repository names, source paths, or incidental implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:13:11.504Z","display_code":"function candidate(left, right, originalOnly) {\n  const textOrder = (a, b) => {\n    if (a === b) return 0;\n    return a > b ? 1 : -1;\n  };\n\n  let result = textOrder(left.source, right.source);\n  if (result !== 0) return result;\n\n  result = left.originalLine - right.originalLine;\n  if (result !== 0) return result;\n\n  result = left.originalColumn - right.originalColumn;\n  if (result !== 0 || originalOnly) return result;\n\n  result = left.generatedColumn - right.generatedColumn;\n  if (result !== 0) return result;\n\n  result = left.generatedLine - right.generatedLine;\n  if (result !== 0) return result;\n\n  return textOrder(left.name, right.name);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0052","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":401,"source_end_line":428,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L401-L428","display_code":"function candidate(mappingA, mappingB, onlyCompareGenerated) {\n  let cmp = mappingA.generatedLine - mappingB.generatedLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.generatedColumn - mappingB.generatedColumn;\n  if (cmp !== 0 || onlyCompareGenerated) {\n    return cmp;\n  }\n\n  cmp = strcmp(mappingA.source, mappingB.source);\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalLine - mappingB.originalLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalColumn - mappingB.originalColumn;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  return strcmp(mappingA.name, mappingB.name);\n}","normalized_hash":"sha256-33a3e5926f2230f9378be183cc08fe49ceaa3447b43979f0ac006319afa40a1e"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:50:42.123Z","content":{"summary":"Compares two mapping-like records to produce a deterministic sort order, optionally considering only generated output position.","signature_intent":"Comparator returning a negative, zero, or positive number for ordering two records.","behavior":["Orders first by generated line position.","If generated lines match, orders by generated column position.","When configured to compare only generated output, stops after generated column comparison.","Otherwise orders by source identifier using string comparison.","Then orders by original line position.","Then orders by original column position.","Finally orders by name using string comparison.","Returns zero only when all compared fields are equal under the selected comparison mode."],"edge_cases":["If generated positions differ, original-position and metadata fields are ignored.","When generated positions match and generated-only mode is enabled, records are treated as equal even if other fields differ.","Missing or non-numeric numeric fields may produce non-standard comparator results according to JavaScript subtraction behavior.","String comparison behavior for missing or non-string fields depends on the string comparison helper."],"forbidden_details":["Internal variable names","Repository names","Source paths","Step-by-step implementation quirks"]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:13:28.193Z","display_code":"function candidate(left, right, generatedOnly) {\n  const generatedLineOrder = left.generatedLine - right.generatedLine;\n  if (generatedLineOrder !== 0) return generatedLineOrder;\n\n  const generatedColumnOrder = left.generatedColumn - right.generatedColumn;\n  if (generatedColumnOrder !== 0 || generatedOnly) return generatedColumnOrder;\n\n  const textOrder = (first, second) => {\n    if (first === second) return 0;\n    if (first == null) return 1;\n    if (second == null) return -1;\n    return first > second ? 1 : -1;\n  };\n\n  const sourceOrder = textOrder(left.source, right.source);\n  if (sourceOrder !== 0) return sourceOrder;\n\n  const originalLineOrder = left.originalLine - right.originalLine;\n  if (originalLineOrder !== 0) return originalLineOrder;\n\n  const originalColumnOrder = left.originalColumn - right.originalColumn;\n  if (originalColumnOrder !== 0) return originalColumnOrder;\n\n  return textOrder(left.name, right.name);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0054","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":455,"source_end_line":482,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L455-L482","display_code":"function candidate(mappingA, mappingB) {\n  let cmp = mappingA.generatedLine - mappingB.generatedLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.generatedColumn - mappingB.generatedColumn;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = strcmp(mappingA.source, mappingB.source);\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalLine - mappingB.originalLine;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  cmp = mappingA.originalColumn - mappingB.originalColumn;\n  if (cmp !== 0) {\n    return cmp;\n  }\n\n  return strcmp(mappingA.name, mappingB.name);\n}","normalized_hash":"sha256-16ca556a928dd44ede9c7c818e8f7e57b957da5ca099fdff71f2d38bdc6b39ac"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:50:59.123Z","content":{"summary":"Compares two mapping records to produce a deterministic ascending sort order by generated position, source identity, original position, and symbol identity.","signature_intent":"Accepts two mapping-like records and returns a negative number, zero, or a positive number indicating their relative ordering.","behavior":["Orders by generated line number first, ascending.","If tied, orders by generated column number, ascending.","If tied, orders by source identifier using string comparison.","If tied, orders by original line number, ascending.","If tied, orders by original column number, ascending.","If tied, orders by symbol name using string comparison.","Returns zero only when all compared fields are equivalent under the numeric and string comparisons."],"edge_cases":["Missing or nonnumeric position values may produce nonstandard numeric comparison results.","Missing string fields are delegated to the string comparison behavior.","Records with identical compared fields are treated as equal for sorting."],"forbidden_details":["No variable names, repository names, source paths, or unnecessary implementation quirks are included."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:13:49.883Z","display_code":"function candidate(left, right) {\n  const numericKeys = [\"generatedLine\", \"generatedColumn\", \"originalLine\", \"originalColumn\"];\n  const textOrder = (a, b) => {\n    if (a === b) return 0;\n    if (a == null) return 1;\n    if (b == null) return -1;\n    return String(a) < String(b) ? -1 : 1;\n  };\n\n  let difference = left[numericKeys[0]] - right[numericKeys[0]];\n  if (difference) return difference;\n\n  difference = left[numericKeys[1]] - right[numericKeys[1]];\n  if (difference) return difference;\n\n  difference = textOrder(left.source, right.source);\n  if (difference) return difference;\n\n  difference = left[numericKeys[2]] - right[numericKeys[2]];\n  if (difference) return difference;\n\n  difference = left[numericKeys[3]] - right[numericKeys[3]];\n  if (difference) return difference;\n\n  return textOrder(left.name, right.name);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0055","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/util.js","source_start_line":499,"source_end_line":545,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/util.js#L499-L545","display_code":"function candidate(sourceRoot, sourceURL, sourceMapURL) {\n  sourceURL = sourceURL || \"\";\n\n  if (sourceRoot) {\n    \n    if (sourceRoot[sourceRoot.length - 1] !== \"/\" && sourceURL[0] !== \"/\") {\n      sourceRoot += \"/\";\n    }\n    \n    \n    \n    \n    \n    sourceURL = sourceRoot + sourceURL;\n  }\n\n  \n  \n  \n  \n  \n  \n  \n  \n  \n  \n  \n  \n  \n  \n  if (sourceMapURL) {\n    const parsed = urlParse(sourceMapURL);\n    if (!parsed) {\n      throw new Error(\"sourceMapURL could not be parsed\");\n    }\n    if (parsed.path) {\n      \n      const index = parsed.path.lastIndexOf(\"/\");\n      if (index >= 0) {\n        parsed.path = parsed.path.substring(0, index + 1);\n      }\n    }\n    sourceURL = join(urlGenerate(parsed), sourceURL);\n  }\n\n  return normalize(sourceURL);\n}","normalized_hash":"sha256-1818898af4d272b05123d34dd7813f94c90f32cf0ac5e7c738f729ac83bb3806"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:51:17.867Z","content":{"summary":"Combines optional base information with a resource reference, resolves it relative to an optional map location, and returns a normalized URL or path.","signature_intent":"Accepts an optional root prefix, a resource reference, and an optional map location, producing a normalized resolved reference.","behavior":["Defaults a missing resource reference to an empty string.","Prepends a provided root prefix to the resource reference.","Adds a single path separator between the root prefix and resource reference when both are relative segments without a boundary separator.","When a map location is provided, resolves the current reference relative to the directory containing that map location.","Returns the resolved reference after normalization."],"edge_cases":["A root prefix ending with a separator is used as-is.","A resource reference beginning with a separator is not given an extra separator.","A missing root prefix leaves the resource reference unchanged before map-based resolution.","An unparsable map location causes an error.","An empty resource reference can still resolve to the provided root or map-relative base."],"forbidden_details":["Internal identifier names.","Repository names or source paths.","Implementation-specific helper names.","Unnecessary parsing or string-manipulation details."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:14:29.321Z","display_code":"function candidate(rootPrefix, resourceReference, mapLocation) {\n  const reference = resourceReference == null ? \"\" : String(resourceReference);\n  let combined;\n\n  if (rootPrefix == null || rootPrefix === \"\") {\n    combined = reference;\n  } else {\n    const prefix = String(rootPrefix);\n    combined = prefix + (prefix.endsWith(\"/\") || reference.startsWith(\"/\") || reference === \"\" ? \"\" : \"/\") + reference;\n  }\n\n  const normalizePath = (input) => {\n    const absolute = input.startsWith(\"/\");\n    const trailing = input.length > 1 && input.endsWith(\"/\");\n    const out = [];\n    for (const part of input.split(\"/\")) {\n      if (part === \"\" || part === \".\") continue;\n      if (part === \"..\") {\n        if (out.length && out[out.length - 1] !== \"..\") out.pop();\n        else if (!absolute) out.push(part);\n      } else {\n        out.push(part);\n      }\n    }\n    let result = (absolute ? \"/\" : \"\") + out.join(\"/\");\n    if (result === \"\" && absolute) result = \"/\";\n    if (trailing && result !== \"/\") result += \"/\";\n    return result;\n  };\n\n  const isUrlLike = (value) => /^[A-Za-z][A-Za-z0-9+.-]*:/.test(value) || value.startsWith(\"//\");\n\n  if (mapLocation != null && mapLocation !== \"\") {\n    const map = String(mapLocation);\n    if (isUrlLike(map)) {\n      let base;\n      try {\n        const parsed = map.startsWith(\"//\") ? new URL(\"http:\" + map) : new URL(map);\n        parsed.hash = \"\";\n        parsed.search = \"\";\n        parsed.pathname = parsed.pathname.replace(/[^/]*$/, \"\");\n        base = parsed;\n      } catch (error) {\n        throw new Error(\"Invalid map location\");\n      }\n      try {\n        const resolved = new URL(combined, base);\n        return (map.startsWith(\"//\") && resolved.protocol === \"http:\" ? \"\" : resolved.protocol) + \"//\" + resolved.host + normalizePath(resolved.pathname) + resolved.search + resolved.hash;\n      } catch (error) {\n        throw new Error(\"Invalid resolved reference\");\n      }\n    }\n\n    const directory = map.replace(/[^/]*$/, \"\");\n    combined = combined.startsWith(\"/\") || isUrlLike(combined) ? combined : directory + combined;\n  }\n\n  if (isUrlLike(combined)) {\n    try {\n      const parsed = combined.startsWith(\"//\") ? new URL(\"http:\" + combined) : new URL(combined);\n      const prefix = combined.startsWith(\"//\") && parsed.protocol === \"http:\" ? \"\" : parsed.protocol;\n      return prefix + \"//\" + parsed.host + normalizePath(parsed.pathname) + parsed.search + parsed.hash;\n    } catch (error) {\n      throw new Error(\"Invalid reference\");\n    }\n  }\n\n  return normalizePath(combined);\n}","retry_count":0}}
{"dataset_version":"v1","pair_id":"js_0056","language":"js","review_status":"approved","license":"BSD-3-Clause","human":{"source_repo":"mozilla/source-map","source_commit":"b2171d58e90e64472b0e858013c0cc5f6772a83d","source_path":"lib/wasm.js","source_start_line":6,"source_end_line":14,"source_url":"https://github.com/mozilla/source-map/blob/b2171d58e90e64472b0e858013c0cc5f6772a83d/lib/wasm.js#L6-L14","display_code":"function candidate() {\n  this.generatedLine = 0;\n  this.generatedColumn = 0;\n  this.lastGeneratedColumn = null;\n  this.source = null;\n  this.originalLine = null;\n  this.originalColumn = null;\n  this.name = null;\n}","normalized_hash":"sha256-f3ef096c135e6715804d149ed66fc7034d5808d3fcf7144db2bc6ea08b22ecbf"},"spec":{"schema_version":"1.0.0","model":"codex","generated_at":"2026-07-04T14:51:25.554Z","content":{"summary":"Creates a new mutable record with default positions and unresolved optional metadata.","signature_intent":"Constructs an instance with initial generated-position state and empty source/original/name metadata.","behavior":["Sets the starting generated position to the first line and first column.","Marks the generated range end as unknown.","Marks associated source metadata as absent.","Marks original-position metadata as absent.","Marks optional naming metadata as absent."],"edge_cases":["Does not accept or use arguments.","Does not return an explicit value.","Requires constructor-style or bound-object invocation to initialize the target object."],"forbidden_details":["Do not expose internal property identifiers.","Do not mention repository names or source paths.","Do not describe implementation-specific ordering beyond observable initialization behavior."]}},"ai":{"model":"codex","generation_prompt_version":"codex-ai-code-v1","generated_at":"2026-07-04T15:14:44.689Z","display_code":"function candidate() {\n  this.generatedLine = 1;\n  this.generatedColumn = 0;\n  this.lastGeneratedColumn = null;\n  this.source = null;\n  this.originalLine = null;\n  this.originalColumn = null;\n  this.name = null;\n}","retry_count":0}}
